# 기기별 로컬 설정 (각 컴퓨터에만 존재, 공유 안 함)
CLAUDE.local.md
.claude/settings.local.json

# Obsidian 앱 설정 (기기별 — 노트 본문(.md)은 공유, 앱 설정은 제외)
.obsidian/

# OS / 편집기 잡파일
.DS_Store
Thumbs.db
desktop.ini

# 로컬 작업 산출물(마을 백업 등) — scripts/village-backup.mjs 의 기본 출력 자리
tmp/

# 시뮬 도구가 잠깐 뜨는 module (scripts/village-sim · 끝나면 지워진다)
village/_simrun-*.mjs
