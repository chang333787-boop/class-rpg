# 큰 판에서 무거워지는 것 — 조사 (2026-09-20 · 마을 프로그램 담당 · **문서만 · 구현 전**)

> 보스: ① 인구 88·167·516 세 판 × 세 거리의 draw call·삼각형·틱 ms·프레임 ms·메모리 ② 어디가 임계인지와 값싼 수 두세 개 ③ 크롬북(저사양·1366×610)에서 무엇이 먼저 아픈지. **규칙 수치는 안 건드린다.**
> 잰 법: 맥북 · Playwright 새 창마다 한 판 · 1366×610 · `?sid=guest&dev=1` · 이미 있는 훅 `__bench(60)`(화면 밖 버퍼에 60프레임 · 사람 움직임 포함)과 `__tickBench(200~300)` · `performance.memory`. 크롬북 흉내는 **CPU 6배 느리게**(CDP `Emulation.setCPUThrottlingRate`). 코드 수정 0.

## 한 줄
**아픈 곳은 '넓게 보기'가 아니라 그 바로 앞 — 한 칸이 12px 언저리일 때다.** 그보다 넓으면 2D 지도로 바뀌어 싸지고(그리기 호출 10~20), 그 문턱 바로 위(배율 5~6)에서 **3D 청크가 한꺼번에 보여** 516명 판이 **그리기 호출 452 · 삼각형 446,090**로 치솟는다. 크롬북에서 먼저 아픈 것은 그림보다 **틱의 튐**(가장 느린 틱 53ms)이다.

## ① 잰 값

**판**

| 판 | 인구 | 걷는 사람 | 놓인 물건 | 구역 | 저장본 | 여는 데(`__LOADMS`) |
|---|---|---|---|---|---|---|
| pop88 | 88 | 88 | 285 | 3 | 3.1KB | 84ms |
| pop167 | 173 | 120(상한) | 390 | 13 | 4.7KB | 87ms |
| **pop529**(창조자 6회 방식으로 키운 판) | 533 | 120(상한) | **19,146** | 52 | 72KB | **202ms** |

**세 거리 · 맥북**

| 판 | 거리(배율) | 그리기 호출 | 삼각형 | 프레임 ms | 틱 중앙 ms | 메모리 MB |
|---|---|---|---|---|---|---|
| pop88 | 넓게 · 지도(1~5) | 6~10 | 20,4xx | 0.44~0.50 | 0.1 | 26~31 |
| pop88 | **3D 가장 넓게(6)** | **63** | 94,611 | 0.52 | 0.1 | 21 |
| pop88 | 가까이(10~14) | 63~79 | 94,739~101,507 | 0.64~0.80 | 0.1~0.3 | 23~30 |
| pop167 | 넓게 · 지도(1~5) | 20~22 | 20,4xx~20,7xx | 0.46~0.59 | 0.3~0.4 | 31~35 |
| pop167 | **3D 가장 넓게(6)** | **90** | 120,956 | 0.73 | 0.3 | 26 |
| pop167 | 가까이(10~14) | 71~83 | 119,674~126,546 | 0.43~0.61 | 0.1~0.3 | 31~39 |
| **pop529** | 넓게 · 지도(1.2~4) | 11~17 | 20,4xx | 0.38~0.62 | 0.2~0.7 | 69~114 |
| **pop529** | **3D 가장 넓게(6)** | **452** | **446,090** | **1.63** | 1.3 | 103 |
| **pop529** | 가까이(10) | 233 | 272,708 | 1.12 | 1.2 | 88 |
| **pop529** | 더 가까이(14) | 138 | 192,309 | 1.03 | 0.5 | 112 |

**CPU 6배 느리게(크롬북 흉내 · CPU만 · GPU는 그대로)**

| 판 | 거리 | 프레임 ms | 틱 중앙 ms | 틱 가장 느림 ms |
|---|---|---|---|---|
| pop88 | 3D 가장 넓게(6) | 3.80 | 1.7 | 9.2~11.0 |
| pop167 | 3D 가장 넓게(6) | 3.47 | 0.7~2.9 | 13.3~18.2 |
| **pop529** | 지도(2.5) | 4.43 | 3.5 | — |
| **pop529** | **3D 가장 넓게(6)** | **10.61** | 6.4 | — |
| **pop529** | 가까이(14) | 6.20 | 6.4 | **53.1** |
| **pop529** | 넓게(1) | 4.87 | 9.3 | **55.2** |

- 메모리는 재는 사이에 GC 로 크게 흔들린다(같은 판에서 69~114MB). **판이 커지면 60MB 대 → 100MB 대**로 올라가는 것만 읽는다.
- 프레임 ms 는 맥북 값이다. **기기와 무관한 값(그리기 호출·삼각형)으로 판단하는 것이 안전하다.**

## ② 어디가 임계인가 · 값싼 수

**임계 셋**
1. **지도 문턱 바로 위(한 칸 ≈ 12~20px · 배율 5~6)** — 3D 청크가 한 번에 다 보인다. 516명 판에서 **452 호출 · 446k 삼각형**. 여기가 창조자의 눈 6회가 본 '457'이다. 더 넓히면 2D 지도라 싸고, 더 좁히면 보이는 청크가 줄어 싸다.
2. **틱의 튐** — 느린 기기에서 가장 느린 틱이 **53~55ms**(516명 판). 60fps 한 프레임(16.7ms)의 세 배라, 그 순간 화면이 한 번 끊긴다. 중앙값(6.4ms)은 견딜 만하다.
3. **메모리** — 516명 판은 100MB 언저리. 시험 중 **창이 한 번 저절로 닫힌 것도 이 판을 되풀이해 열 때**였다(보스께 보고한 그 건).

**값싼 수(고르는 것은 보스 · 규칙 수치는 안 건드린다)**

| # | 수 | 효과(예상) | 비용·걱정 | 크기 |
|---|---|---|---|---|
| 1 | **지도 문턱을 조금 올린다**(한 칸 12px → 16~18px) | 최악 구간(배율 5~6)이 **통째로 사라진다** — 그 거리에서 452 → 15 안팎 | 아이가 조금 더 일찍 '그림 지도'를 본다. **디자인·창조자의 눈 확인 필요**(보기 좋은가) | 한 줄 |
| 2 | **멀리서는 작은 꾸밈을 뺀다(LOD)** | 청크를 만들 때 '작은 꾸밈'(꽃·벤치·울타리 같은 1×1)을 따로 묶고, 배율 < 8이면 안 그린다 → 호출·삼각형 함께 준다. 516판 446k 중 큰 몫이 꾸밈 | 청크 만드는 코드가 한 겹 늘어난다. 잘못 묶으면 그림이 달라 보인다 | 중간 |
| 3 | **청크를 더 크게 묶는다**(6 → 8칸) | 그리기 호출이 **대략 (6/8)² = 0.56배** — 452 → 250 안팎 | 한 칸 고칠 때 다시 만드는 조각이 커져 **놓는 순간이 느려질 수 있다**(재 봐야 한다 · 전에 4 → 6 으로 올린 적 있음 #661) | 작음 |
| 4 | (미측정) 넓게 볼 때 **그림자 끄기** | 그림자는 장면을 한 번 더 그린다 — 넓은 거리에서 크다 | 낮 그림자가 사라져 평평해 보인다. **아직 안 쟀다** | 작음 |
| 5 | (미측정) 틱의 튐 낮추기 | 가장 느린 틱(53ms)이 어디서 나는지부터 — 1/90 조각 훑기(붐빔·일자리)가 한 틱에 몰리는지 본다 | 규칙 수치가 아니라 **조각 나누는 수**만 건드리는 범위인지 먼저 확인 | 조사 먼저 |

→ 추천 순서: **1(문턱) 재 보기 → 3(청크 8) 재 보기 → 2(LOD)**. 1·3은 한 줄씩이라 실제로 재서 고르면 된다. 4·5는 재 본 뒤 따로.

## ③ 크롬북(저사양 · 1366×610)에서 먼저 아픈 것
1. **틱의 튐(53~55ms)** — 그림보다 먼저 느껴진다. 사람이 걷다 한 번 멈칫한다. 큰 판에서만 난다.
2. **3D 가장 넓게(배율 5~6)의 그리기 호출** — 516명 판에서 452. 프레임 10.6ms(CPU 6배)는 GPU 가 약한 기기에서 더 나빠진다(여기 GPU 는 맥북 것이다 — 실제 크롬북은 이보다 나쁘다고 봐야 한다).
3. **메모리 100MB** — 탭이 닫히는 위험. 실제로 한 번 겪었다.
4. 첫 열기(202ms)와 저장본 72KB 는 아직 문제 아니다.

## 안 한 것 · 조심할 것
- 규칙 수치(VRULES·판정)는 **한 글자도 안 건드렸다**. 이 문서는 그림·거리·청크 이야기다.
- 맥북에서 쟀다. CPU 6배 느리게는 **CPU만** 느리게 한 것이라 크롬북 GPU 를 대신하지 못한다. 그래서 **그리기 호출·삼각형**을 기준으로 삼았다.
- `__bench` 는 화면 밖 버퍼에 그린다(아이 화면과 같은 장면 · 같은 사람 수). 실제 화면의 vsync·합성 비용은 안 들어 있다.
- 516명 판은 창조자의 눈 6회 방식으로 키운 판이다(아이가 실제로 만든 판이 아니다 — 길이 바둑판이라 실제보다 고를 수 있다).
- 메모리는 GC 때문에 흔들린다. 같은 판에서 69~114MB 로 읽혔다.
