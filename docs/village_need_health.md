# 현대 마을의 필요 — '물(우물)'을 빼고 '건강(🏥 동네 의원)'을 넣는다 — 설계 한 장

> 2026-09-24 · 고장 프로토 담당 · **문서만 · 코드 0**
> 결정: 보스 09-24. 방향은 사용자가 직접 줬습니다 — "조선시대면 몰라 현대시대에 뭔 우물 때문에 물이 부족해" · "우물 자체가 짜친다".
> 기준 코드: `main` 0707089(09-24 오후) · 줄 번호는 그때 것입니다.

## 0. 한 줄

지금 마을의 기본 필요는 **물 · 장보기 · 놀이 · 쉼** 넷입니다. 여기에 인구 80명부터 배움이 붙습니다.

현대 마을에서는 **물을 뺍니다.** 물은 수도로 집마다 늘 나온다고 가정합니다(판 카드 18칸 '가정하는 것').

빈 자리는 **건강 — 🏥 아플 때 갈 곳(동네 의원)** 이 채웁니다. 건강은 배움처럼 **마을이 커지면 생기는 필요**입니다(주민 N명부터 · 제안 N = 100).

우물 kind 는 지우지 않습니다. 판정 없는 **'옛 우물' 장식**으로 남겨, 옛 저장본을 그대로 열고 나중 '옛날과 오늘날' 판에서 씁니다.

교과 근거:
- [4사01-02] 해설의 편익 시설 넷(놀이·여가 / 안전 / 교육·문화 / **건강·의료**)에 물은 없습니다(`docs/village_curriculum_map.md` 41줄 · 건강: 보건소·병원 ← 겨울에 기침하며 천천히 걷는 주민).
- 우물은 3학년 (4) '옛날과 오늘날의 생활 모습'의 **옛날 모습**(→ 수도)으로 나옵니다.

---

## 1. 새 뜻 (보스 결정 1~6)

| # | 무엇 | 엔진에서 |
|---|---|---|
| 1 | 물은 수도로 집마다 늘 나온다 | 필요 목록에서 '물'을 뺀다(`NEEDS_BASE` · `AWAY_NEEDS`) · 판 카드 18칸 '가정하는 것'에 적는다 |
| 2 | 빈 필요 자리 = **건강**(아플 때 갈 곳 · 동네 의원·보건소) | 새 필요 '건강' · 새 kind '의원'(6절) |
| 3 | 건강은 날마다 가는 곳이 아니다 | **닿기 판정은 같다**(길로 14칸 · 큰길 20) · 발걸음은 아픈 주민만 가끔(연기는 주민 연기 몫 — 판정 0) |
| 4 | 마을이 커지면 새 필요(배움 80 과 같은 꼴) | 기본 필요는 **장보기·놀이·쉼** 셋 · 건강은 **주민 N명부터**(3절) |
| 5 | 우물 kind 는 지우지 않는다(저장본 보호) | `need` 를 없애 **판정 없는 '옛 우물' 장식**으로 · 현대 판의 놓기 메뉴에서만 뺀다 · 나중 '옛날과 오늘날' 판에서 쓴다 |
| 6 | 분수·연못은 '물'을 빼고 쉼만 | 분수 `need:'물'` → `need:'쉼'` · 연못 `need:'물', need2:'쉼'` → `need:'쉼'` |

---

## 2. '물'·우물이 닿은 곳 — 전수(①)

### 2-가. `village/index.html`

| 줄 | 무엇 | 바꿀 것 | PR |
|---|---|---|---|
| 526 | `K.well` — `nm:'우물' · need:'물' · grp:'건물'` | `need` 없앰 · 이름 **'옛 우물'** · 현대 메뉴에서 뺌(kind·모양은 그대로) | 1 |
| 538 | `K.pond` — `need:'물', need2:'쉼'` | `need:'쉼'`(need2 없앰) | 1 |
| 548 | `K.fountain` — `need:'물'` | `need:'쉼'` | 1 |
| 882 | 트레이 묶음 '🏠 집·가게' 에 `well` | 현대 메뉴에서 뺌 → 의원(PR 2)으로 | 1 · 2 |
| 1010 · 11754 · 11737 · 11778 | 시연 판(#full · #adv · #demo)이 우물을 깐다 | 그대로 두거나 의원으로 — 시연용(저장 안 함) | 4 |
| 2389 | `SAY_ICON` 물 🪣(집 위 필요 풍선) | 물 빼고 **건강 🏥** | 1 · 2 |
| 3181 · 3183 | `waitSayJob` 의 `4 - rest.length` — **필요 넷이 박혀 있음** | `AWAY_NEEDS.length` 로 | 1 |
| 4042 | `PCOUNT_ICON` 물 💧(L3 '한 수의 대답') | 물 빼고 건강 🏥 | 1 · 2 |
| 4160 · 4161 | `VOICE_KEYS` · `VOICE_TXT` 물 '🪣 물 뜰 곳'(📣 동네 바람표) | 물 빼고 건강 '🏥 아플 때 갈 곳' | 1 · 2 |
| 4162 | `VOICE_MARK`(동네 이름을 짓는 눈에 띄는 건물)에 `well` | 그대로 둬도 됨(옛 우물도 동네 표지) — 보스 판단 | 4 |
| 4493 · 4499 | 돌아섬 말 `SAYAWAY_FIX` 물:'우물' · `SAYAWAY_ORDER` | 물 빼고 건강:'의원' | 1 · 2 |
| 5673 | `hourPref` 6~9시 = '물'(아침 물 뜨러 감) | 물 빼면 아침 끌림 없음 — 주민 연기가 정함 | 4(연기) |
| 5954 | `turnAway.minHave: 2` — '넷 중 둘은 있어야 이사 옴' | 셋이 되면 **'셋 중 둘'** — 더 빡빡해짐(3절 표의 pop88 인구 −1) · 보스 판단(7절) | 1 |
| 5955 · 5956 | `AWAY_NEEDS` · `AWAY_ICON` 에 물 | 물 뺌(건강은 돌아섬에 넣지 않음 — 7절) | 1 |
| 6048 | `waitSay` 아이콘 표 · `4 - miss.length` | 물 빼고 · `AWAY_NEEDS.length` | 1 |
| 6094 · 6095 | `NEEDS_BASE = ['물','장보기','놀이','쉼']` · `LEARN_POP = 80` | `['장보기','놀이','쉼']` · 건강은 `HEALTH_POP`(3절) | 1 · 2 |
| 6096 | `NEED_TXT` 물 '물 뜰 곳' | 물 빼고 건강 '아플 때 갈 곳' | 1 · 2 |
| 6101 | 배움이 켜지는 한 줄(인구 80 · 소식 '📚 마을이 커졌어요 — 이제 배울 곳도 바라요') | 같은 꼴로 건강: '🏥 마을이 커졌어요 — 이제 아플 때 갈 곳도 바라요(동네 의원)' | 2 |
| 6109 | `computeSat` 의 `met` 처음 값에 물 | 건강 더함 | 2 |
| 6900 | 목표 `well` '우물 하나 놓기'(기본 마을 앞 목표) | **바꿈** — 안: '긴의자 하나 놓기 — 쉴 곳'(id `bench1`) · 옛 저장본의 `well` id 는 그대로 둠(4절) | 4 |
| 7276 | `whatDoing` '에 물 뜨러 가요' | 그 줄 없앰(분수·연못은 '쉬어요') | 4 |
| 7640 | 친구 마을 시연(구경) 판이 우물을 깐다 | 의원 또는 가게로 | 4 |
| 8181 · 8189 | 3D 필요 표지(`ICON.물` 파란 물방울 · 없으면 '물'로 떨어짐) | 건강 표지(모양은 디자인) · 기본값을 '물'이 아닌 것으로 | 2 · 디자인 |
| 9006 | `WHY_ICON` 물 🪣(왜 풍선) | 건강 🏥 | 1 · 2 |
| 9237 · 9253 | [ACT-WELLCHAT] 아침 우물가 수다(well·fountain·pond · 6~9시) | 물을 안 뜨니 저절로 안 모인다 — 옛 우물 곁 수다로 남길지 **주민 연기가 정함** | 4(연기) |
| 10436 · 10479 · 10544 | [ACT-BESIDE] 우물에서 나오면 물동이 · '우물가에서 물을 길어요' | 주민 연기가 정함(옛 우물은 들르지 않음) | 4(연기) |
| 10695 · 10700 | [MAC-STAGEOFF] 주석의 우물 예 | 글만 | 4 |
| `STAGE_HOOKS['다있음%']`(10686 무렵) | `NEEDS.every` — **저절로 따라감**(셋 · 건강이 켜지면 넷) | 코드 0 · 판 목표 값만 움직임 | — |
| 12480 | 모형 화면 `PMAP_ORDER` 에 `well` | proto 판과 함께 | 3 |
| `__destMix` · `__needs` · `__forceLearn` 등 시험 훅 | 물 칸 | 건강 칸 · `__forceHealth` | 1 · 2 |
| 주석 44곳의 '우물' | 설명 글 | 고친 줄만 따라 고침 | — |

첫 안내 세 장(`GUIDE_CARDS`)에는 우물이 **없습니다**. 길 · 집 · 목표 이야기뿐이라 고칠 것이 없습니다.

### 2-나. 판 파일 · 시작 땅 · 저장본

| 파일 | 우물·물 | 바꿀 것 | PR |
|---|---|---|---|
| **town3 · town3-origin** | 질문·역할·예상·현상에 '우물' · 목표 '주민이 쓸 수 있는 우물 하나 (집에서 닿게)' · 건물 `well` | **우물 → 의원**(편익 시설 '건강·의료'와 딱 맞음) · 판 규칙으로 건강을 처음부터 켬(`health.pop: 0`) · 예상 물음 '가게·놀이터·의원을…' | 3 |
| **nowater**(#1020 '물이 모자란 동네 — 우물이 없는 골목') | 판 전체가 우물 | **판을 바꿈**: 안 — '아플 때 갈 곳이 먼 동네 — 의원이 없는 골목'(`nohealth` · 시작 땅은 우물 3개 → 옛 우물 장식 · 의원 0) · 만든 담당과 보스 결정 | 3 |
| **proto-vote**(정치 프로토 '우물을 어디에 놓을까') | `결정.시설: 'well' · 이름: '우물' · 필요: '물'` · 의견 글 | 결정할 시설을 **의원**으로(`결정` 데이터만 · VOTE 코드는 데이터를 읽음) — **정치 프로토 담당 결정** | 3 |
| onebridge | 한 곳 '우물' 글 | 글만 | 3 |
| city · farm · mountain · sea · proto-flow | 건물 목록에 `well` | 뺌(현대 메뉴) · 필요하면 의원 | 3 |
| 시작 땅 `starts/` 여섯(nowater · onebridge · origin · proto-flow · proto-vote · town3-origin) | 우물이 놓여 있음(`palette`) | **그대로**(옛 우물 장식으로 열림 · 4절) — 모습만 바꿀지는 디자인 | — |
| 저장본 `boards/` 셋(pop88 · pop167 · mid36) | 우물 · pop88·pop167 은 목표 `well` 이룸 | 그대로(시뮬 기준 판 — 4절과 같음) | — |

### 2-다. 문서 · 수업 카드 · 용어 · 창조자 목록

- 우물이 많은 문서: `village_stage_nowater.md`(24) · `village_proto_vote_design.md`(18) · `village_progression_plan.md`(13) · `village_first_screen.md`(9) · `village_lesson_flow.md`(8) · 창조자 기록 여럿.
  - 설계 문서는 판을 고치는 PR(3)에서 같이 고칩니다. 창조자 기록은 **기록이라 고치지 않습니다**.
- `village_terms.md` · `village_stage_card.md`: 18칸 '가정하는 것'의 보기로 "물은 수도로 집마다 늘 나온다(현대 마을)"를 적습니다.
- `village_curriculum_map.md`: 41줄 '건강: 보건소·병원'이 이제 기본 엔진에 들어온다고 한 줄 적습니다.
- 어설픔 목록(`docs/clumsy_list.md` · 창조자만 고침)의 우물 줄(예: 28-ⓑ60 '첫 우물의 L3 💧 11채')은 창조자에게 넘깁니다. 물이 빠지면 그 줄은 '의원 · 🏥'로 되밟게 됩니다.

---

## 3. N 제안 — 건강은 주민 몇 명부터(②)

**뜻**: 배움은 '아이가 늘면 배울 곳'이라 80명이고, 도서관은 50명에 먼저 열립니다. 건강은 '사람이 많아지면 아픈 사람도 생긴다 · 동네 의원은 동네 여럿에 하나'입니다.

- 아이에게 설명하기 쉬운 둥근 수 **100명**을 제안합니다: "우리 마을이 100명이 되면 아플 때 갈 곳도 있어야 해요".
- 의원은 배움처럼 **필요보다 먼저 연다**(해금 인구 80 · 역·녹지와 같은 줄). 그래야 필요가 생기기 전에 놓아 둘 수 있습니다.
- 수업 판(3학년 동네 판)은 판 규칙으로 **처음부터** 켤 수 있게 합니다(`VRULES.health.pop` 을 판이 0으로).

**잰 값** — 표준 판 12 × 시드 1-3 × 2일(지금 main · 끝 값은 세 시드 평균):

| 판 | 인구 시작 → 2일 뒤 | N=60 | N=80 | **N=100** | N=120 |
|---|---|---|---|---|---|
| 기본+pop88 | 88 → 95.3 | 켜짐 | 켜짐 | — | — |
| 기본+pop167 | 173 → 184 | 켜짐 | 켜짐 | **켜짐** | 켜짐 |
| farm+mid36 | 74 → 74 | 켜짐 | — | — | — |
| town3-origin | 74 → 74 | 켜짐 | — | — | — |
| origin | 74.3 → 86.7 | 켜짐 | 이틀째 켜짐 | — | — |
| town3 · proto-flow · proto-vote | 28 · 12 · 16 | — | — | — | — |
| farm · city · sea · mountain(빈 땅) | 0 | — | — | — | — |
| **켜지는 판** | | 5 | 3 | **1** | 1 |

- N=100 이면 기본 판에서는 **pop167 급 큰 마을만** 건강이 생깁니다. 수업 판은 판이 스스로 켤 때만 생깁니다.
- 학교 해금이 120 이라, 120 이면 두 가지가 한꺼번에 옵니다.
- 학생 마을의 실제 인구 분포는 **재지 않았습니다**(운영 DB 를 읽지 않음). 교실에서 한 주 뒤 인구를 보고 다시 맞출 값입니다.

---

## 4. 옛 저장본을 열면(③) — 데이터 유실 0

| 저장 칸 | 옛 저장본(우물 있는 guest · 학생 localStorage · 원격 Firebase) | 새 코드에서 |
|---|---|---|
| `palette` · `plotStr`(물건) | `well` · `fountain` · `pond` 가 있음 | **kind 가 그대로 있어 모두 되살아남**(못 살린 것 0) · 우물은 '옛 우물' 장식 · 분수·연못은 쉼 |
| `houses`(식구·층) | 그대로 | 그대로 — 필요는 저장하지 않고 **열 때 다시 셈**(물이 없어지니 만족이 다시 계산될 뿐) |
| `goals` | `well` 이 이룬 목표에 있음(pop88 · pop167 · 시작 땅 다섯) | id 가 목록에서 빠져도 `goalsDone` 에 남아 **무해**('이룬 것 N개' 셈에만 하나) · 지우지 않음 |
| `unlocked` | 우물·분수·연못 | 그대로(해금은 막지 않음) |
| `hist`(마을 흐름 점) | `{ d, 인구, 만족, 일자리 }` — **필요별 칸이 없음** | 그대로(물이 빠진 날부터 만족 값이 바뀔 뿐) |
| `배움: 1`([MAC-LEARNSAVE]) | 그대로 | 그대로 |
| 판 카드 기록(`rpg.stagecard.*`) | 예상 글·지난 시도에 '우물' 이 있을 수 있음 | 글이라 그대로 보임 — 판 파일 예상 물음만 새로(옛 기록은 그 판을 ↺ 하면 새로) |
| **원격(sync.js)** | `fromVillage`·`toVillage` 가 **정한 칸만** 나름(`palette · plotStr · houses · unlocked · goals · hist …`) | 우물은 물건 칸이라 그대로 오감 · **Firebase 구조 변경 0** |

- 건강 저장 칸(PR 2): 배움과 같은 꼴로 `건강: 1`(한 번 켜지면 인구가 줄어도 안 꺼짐)을 적을지는 **보스 결정**입니다(저장 구조 변경 — 7절).
  - 적지 않아도 열 때 인구가 N 이상이면 곧 다시 켜집니다. 다른 점은 N 아래로 줄었을 때뿐입니다.
  - 원격(sync.js)은 `배움` 도 나르지 않습니다(알려진 한계). 건강도 같습니다.
- **지우는 것은 하나도 없습니다.** 우물 kind · 저장 칸 · 원격 구조 모두 그대로라 되돌리기(스위치 끔)도 저장본을 건드리지 않습니다.

---

## 5. PR 순서 · 판마다 예상 vsref 차이(④)

| PR | 무엇 | 스위치 | 예상 vsref 차이(전부 의도한 차이) |
|---|---|---|---|
| **1 물 끔** | 필요 목록에서 물을 뺌(`NEEDS_BASE` · `AWAY_NEEDS` · 넷 박힌 곳 둘) · 우물 need 없앰('옛 우물') · 분수·연못은 쉼 · 현대 메뉴에서 우물 뺌 · 아이콘 표에서 물 뺌 | `VRULES.needWater`(끄면 옛 넷) | 아래 표 — **잰 값** |
| **2 건강** | 새 필요 '건강'(N=100 · 해금 80) · 새 kind '의원'(6절 · 모양은 자리표) · 말 '🏥 아플 때 갈 곳' · 소식 한 줄 · 시험 훅 | `VRULES.health = { on, pop: 100 }` | pop167 만(의원이 없는 채로는 모든 집이 건강을 못 채움 → 웃는집·만족이 내려감) — **PR 2 에서 잰다** |
| **3 판·수업 판** | town3·town3-origin 우물 → 의원(처음부터 켬) · nowater → nohealth 안 · proto-vote 결정 시설 · 건물 목록 · 예상 글 | 판 파일 | 그 판들만 · #1018 이 '⚠ 바뀐 판'으로 스스로 보여 줌 |
| **4 말·연기·시연** | 목표 `well` → `bench1` 안 · `whatDoing` · 시연 판 · 친구 마을 · 우물가 수다·물동이(**주민 연기 담당**) · 문서 | — | 목표 순서가 바뀌면 기본 판 목표 셈만 |

**PR 1 의 잰 값** — `NEEDS_BASE` · `AWAY_NEEDS` 에서 물만 뺀 사본 vs 지금(표준 판 12 × 시드 1-3 × 2일 · 끝 값 평균):

| 판 | 값 | 다른 값 | 끝 인구 지금→물없음 | 웃는집 | 찡그린집 | 돌아선집 |
|---|---|---|---|---|---|---|
| 기본+pop88 | 1725 | **298** | 95.3 → 94.3 | 0 → 0 | 32 → 29 | 14 → 11.3 |
| 기본+pop167 | 1725 | **273** | 184 → 183 | 0 → 0 | 64 → 60 | 0 → 0 |
| farm+mid36 | 2475 | **450** | 74 → 74 | 0 → 0 | 25 → 26 | 5 → 5 |
| town3 | 1875 | **75** | 28 → 28 | 0 → 0 | 14 → 14 | 0 → 0 |
| town3-origin | 1800 | **150** | 74 → 74 | 0 → 0 | 33 → 34 | 4 → 4 |
| origin | 1575 | **190** | 86.7 → 85.3 | 29.3 → 29 | 0 → 0 | 0 → 0 |
| proto-flow | 2100 | **75** | 12 → 12 | 0 → 0 | 1 → 2 | 0 → 0 |
| **proto-vote** | 1650 | **307** | **16 → 26.3** | 0 → 4.7 | 0 → 0 | 0 → 0 |
| farm · city · sea · mountain | — | 0 | 0 | | | |

- 사람이 사는 판은 모두 달라집니다.
- **proto-vote** 는 우물 자리를 고르는 판이라, 물이 없으면 고르기 전에도 마을이 자랍니다(판의 전제가 사라짐 → PR 3 에서 결정 시설을 바꿔야 함).
- pop88 · pop167 · origin 은 물을 빼니 인구가 **조금 줄었습니다**(−1 · −1 · −1.4). 까닭 가설: 돌아섬이 '넷 중 둘'에서 '셋 중 둘'로 빡빡해짐(물은 흔히 채워지던 필요였음). 7절에서 정할 일입니다.
- 이 표는 스위치 없는 사본으로 잰 것입니다. PR 1 은 같은 값을 `VRULES.needWater` 로 냅니다(vsref `--allow` · '의도한 차이'로 적음).

---

## 6. 의원 kind(⑤) — 모양은 마을 디자인 시안

| 칸 | 제안 | 까닭 |
|---|---|---|
| id · 이름 · 그림 | `clinic` · **'의원'**(아이 말: 동네 의원) · 🏥 | '보건소'는 행정 용어라 3학년에게 덜 익숙 — 판 글에서는 '병원·보건소'도 함께 부름 |
| 크기 | **2×2** | 가게·도서관·우물과 같은 동네 시설 크기(문 한 쪽) |
| 필요 | `need: '건강'` | 닿기 판정은 다른 시설과 같음(길로 14칸 · 큰길 20) |
| 길 | `needRoad: true` | 가게·도서관처럼 길에 붙어야 |
| 일자리 | `jobs: 2`(의사·간호사) | '일터마다 일자리 2~4 · 직업 서열 없음'(`village_curriculum_map.md` 지키는 것) |
| 해금 | 인구 **80** | 필요(100)보다 먼저 — 도서관 50 → 배움 80 과 같은 꼴 |
| 수 한도 | `cap: 6` | 큰 마을(구역 여럿)에 동네마다 하나 · 도서관처럼 한 개로 막지 않음 |
| 발걸음 | 날마다 안 감 — 아픈 주민만 가끔(보스 결정 3) | **주민 연기 담당** 몫 · 판정 0 |
| 모양 | 자리표(흰 벽 · 붉은 십자) | 마을 디자인이 시안을 냄 — 판정·저장과 무관 |

---

## 7. 보스가 정할 것

1. **N = 100 · 의원 해금 80**(3절)으로 갈지.
2. **돌아섬 기준**: 물이 빠지면 '셋 중 둘'이 됩니다.
   - (가) 그대로 두기 — 잰 값으로는 인구 −1 안팎
   - (나) '셋 중 하나'로 풀기
   - 건강이 켜진 마을에서도 돌아섬은 셋으로만 셀지(건강은 이사 오는 까닭이 아니라 사는 동안의 필요 — 제안: 셋)
3. **건강 저장 칸 `건강: 1`**(배움과 같은 꼴 · 저장 구조에 칸 하나)을 둘지. 두지 않으면 인구가 N 아래로 줄 때 필요가 꺼졌다 켜집니다.
4. **nowater 판**을 nohealth('의원이 없는 골목')로 바꿀지 — 만든 담당과.
5. **proto-vote** 결정 시설을 의원으로 — 정치 프로토 담당과.
6. 기본 마을 목표 `well`('우물 하나 놓기')을 `bench1`('긴의자 하나 놓기 — 쉴 곳')으로 바꿀지.
7. 우물가 수다·물동이 연기(주민 연기)를 옛 우물 곁 장면으로 남길지.

## 미검증

- 학생 마을의 실제 인구 분포(N 을 맞출 근거) — 운영 DB 를 읽지 않았습니다.
- 물이 빠진 뒤 아이가 '쉴 곳·놀 곳'을 더 놓는지(우물이 가장 먼저 놓던 시설이었음) — 교실에서만.
- 건강 필요(PR 2)의 판정 값 — 의원이 없는 채로는 모든 집이 못 채웁니다. 그 크기는 PR 2 에서 잽니다.
- '의원'이라는 이름이 3학년에게 '병원'보다 나은지 — 교사 확인.
