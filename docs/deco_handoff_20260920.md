# 꾸미기(마당·집 안) 인계 — 2026-09-20 맥북 마감

> 사용자 결정(09-20): **마을(`village/`)은 맥북에서만 개발하고, RPG의 나머지는 학교에서 한다.** 꾸미기는 오늘 맥북에서 여기까지 하고 멈춘다. 이 문서는 학교 세션이 이어받기 위한 한 장이다.
> 오늘 올린 것은 전부 `main`(운영)에 들어갔고, 열린 꾸미기 PR은 없다.

## 오늘 들어간 것 (전부 운영)
| 커밋 | 무엇 |
|---|---|
| `c133cec` (#683) | **집 안 벽지·바닥 고르기** — 마당 바닥 고르기와 **같은 판·같은 조각(`_pk*`)** 재사용. 벽지 8·바닥 4 + 색. 저장 `indoor[공간].look = "바닥,벽지"`(이름#색) |
| `c5f6d48` (#684) | 교사 화면 **수치 초기화가 `s.indoor` 도 비운다**(한 명·전체 두 곳) |
| `0a5c763` (#686) | **벽걸이는 벽 띠에** — 액자는 0번 줄 저장 그대로 두고 그림만 한 칸 위 벽에. 괘종시계는 `DECO_WALL` 에서 빠져 어느 줄에나 |
| `277bb6c` (#692) | **집 안 방 만들기(IN-3)** — 🖌️ 판 세 번째 탭 ⬛ 방 · 한 손가락 네모 끌기 · 크기 칩(작은·보통·큰) · 🪑 가구 둘레로 · 🗑️ 방 없애기(가구는 남음) · 옆으로 붙은 방 사이 문 자동 · 방 밖은 빈 터(도면) |
| 앞선 것들 | 상점 [🎒 내 것 \| 🛒 상점] 탭과 [🎒 가방에 넣기] · 마당 바닥 고르기 · 바닥 테두리 색 · 네모로 칠하기(400칸) · 장식 98종 축척·화풍 감사와 다시 그리기 · 겹치는 장식 9종 상점에서 숨김(`hidden:true`) |

**저장 구조(집 안)**
```
student.indoor["<공간>"] = {
  look:  "바닥,벽지",                       // 이름#색
  rooms: { a: "r,c,w,h,바닥,벽지", ... }    // 방 한 칸당 ≈28B · 최대 8방 · 4×3 ~ 20×12
}
```
못 읽는 값은 읽을 때 버린다. 방이 없는 집 안은 예전과 **픽셀 단위로 같다**(하네스가 지킨다).

## 다음 후보 (아직 손대지 않음 · 순서 추천)
1. **방 크기 바꾸기 손잡이** — 지금은 만들 때만 크기를 정한다. 겹침 규칙(`_inRoomOverlap`·벽 띠 줄까지)이 이미 있으니 붙이기 쉽다.
2. **위아래로 붙은 방의 아치 문** — 규칙 문서가 "IN-3 뒤 시안"이라고 적어 두었다. 디자인 시안 먼저.
3. **IN-4 창·낮밤** — 집 안에도 창과 시간대.
4. 벽걸이 종류 늘리기(벽난로·시계·수족관·창틀 시안 12장이 저장소 밖에 있다 — 사용자 고르기 대기).

## 이어받을 때 지킬 것
- 검증 3종: `node scripts/unit/precheck.mjs --base origin/main`(기대 **PASS 13 · REVIEW 1 · FAIL 0**) · `node scripts/unit/deco-save-count/run.mjs`(줄 수·`=false` 0·끝 표식 `걸린시간_초=` 확인 — 09-20 기준 274줄) · `node scripts/smoke-test.mjs`(29/0).
- **옛 저장본이 그대로 열리는지**와 **아무것도 고르지 않은 집 안이 예전과 같은지**(픽셀 A/B)를 매번 본다.
- `student.js` 를 고치는 PR은 한 번에 하나. 버스터(`student.js?v=`·`student.css?v=`)는 보스가 충돌을 정리한다(09-20 기준 `20260920mcx`).
- 운영 DB로 `student.html` 을 열지 않는다 — 놀이판(`scripts/unit/deco-save-count/play.mjs`, 가짜 프로젝트)에서 본다.
- 크롬북 1366×610 · 태블릿 터치 44px에서 직접 눌러 본다. 한 손가락은 도구, 두 손가락은 언제나 화면 이동.
- 꾸미기는 **시뮬이 아니다** — 점수·목표·필요를 넣지 않는다. 잣대는 ①더 예뻐지는가 ②살아 움직이는가 ③"이런 것도 돼?" 하고 놀라는가.

## 참고 문서
`docs/indoor_rooms_proposal_20260920.md`(IN-1~5) · `docs/indoor_look_rules_20260920.md` · `docs/deco_floor_picker_20260920.md` · `docs/deco_floor_edge_colors_20260920.md` · `docs/deco_shop_home_screens_20260920.md` · `docs/deco_living_yard_20260920.md`
