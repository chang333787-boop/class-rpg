# 마을 — 쌓아 두는 목록 (당장 안 고치는 것)

> 보스 규칙(2026-09-21): 관찰에서 나온 자잘한 것은 **'아이가 틀린 것을 배우는가'에 해당할 때만 즉시 고친다.** 나머지는 여기에 쌓는다.
> 한 줄 꼴: **무엇 · 어디서 나왔나 · 왜 당장 안 하나 · 꺼낼 조건**. 고치면 줄을 지우지 말고 `~~취소선~~` + PR 번호를 남긴다.

## 판단 기준 (이 목록에 넣을지 / 바로 고칠지)

| 바로 고친다 | 쌓아 둔다 |
|---|---|
| 아이가 **틀린 것을 배운다**(화면 말과 실제가 다르다 · 시킨 대로 했는데 나빠진다) | 어른 눈에만 보이는 어긋남 |
| 아이가 **고칠 방법이 없다**(막다름) | 고칠 방법은 있는데 불편하다 |
| 저장본·운영 안전 | 그림·연기의 다듬기 |

---

## 쌓인 것

| # | 무엇 | 어디서 | 왜 당장 안 하나 | 꺼낼 조건 |
|---|---|---|---|---|
| 1 | **`mode: 'front'` 가 한 칸만 보아 과장한다** — 집 바깥쪽에 길을 한 줄 깔면 문 앞이 한산한 새 길로 바뀌어 붐빔이 0 이 된다 | 붐빔 제안서 ④(#730) · 보스 지시로 이번엔 안 건드림 | 자기 몫 빼기(#732)로 과장이 줄었다. 근본 고침('좁은 구간 평균')은 **안 재 봤다** | 10회 관찰에서 '길 한 줄이 너무 잘 듣는다'가 나오면 |
| 2 | **돌아섬 말이 실제 장면에서 미검증** — `JOB_WHY` 로 갈린 말이 돌아서는 이웃에게 제대로 뜨는지 코드 경로만 이었다 | R01(#721) · 보스가 9회 관찰에 맡김 | 재현 판을 못 만들었다(빈집 앞 돌아섬 장면) | 창조자의 눈이 실판에서 그 장면을 만들면 |
| 3 | **`cut` 을 따라 길을 이은 뒤 `full` 이 뜨는 두 걸음이 답답한지** | 9회 B(#729) | 설계상 의도한 두 걸음(먼저 길, 그다음 자리) | 10회에서 '길을 이었는데 또 안 된다'가 나오면 |
| 4 | **`far` 가 실판에서 0채** — 죽은 말은 아니지만(뱀 길 판에서 난다) 아이 판에서 얼마나 나오는지 모른다 | 9회 B(#729) | 지우면 그 경우가 `none` 으로 잘못 말한다(#721 이 고친 문제) | 아이 판 통계(`run.mjs --show 까닭`)에서 계속 0 이면 문구 손질 |
| 5 | **자동 소식 일부가 아직 `toast` 를 바로 부른다** — 14곳은 옮겼지만 남긴 넷(사진 카드가 가로채는 📰 · 글자를 되읽는 칭호 귀띔 · 새 판본 알림 · 손짓 뒤 늦은 안내) | 말 대기줄(#722) | 옮기면 **그 장치들이 깨진다**(주석에 까닭 적어 둠) | 그 장치를 손볼 일이 생길 때 함께 |
| 7 | **중간 크기 판(집 20~40채)에서 붐빔 자기 몫 빼기 효과 미측정** | 붐빔 제안서(#730·#732) | 작은 판·큰 판 둘은 쟀다 | 중간 크기 재현 판이 생기면 |
| 8 | **아주 큰 판(19,146 물건)에서 새 셈들의 비용 미측정** — 멀티 소스 BFS(#728) · 재배정(#723) · 까닭 넷(#721) | 각 PR '확인 못 한 것' | 실학급 크기에서는 틱 최대 1.5~2ms 로 문제없다 | 아이 판이 물건 수천 개가 될 때 |
| 9 | **판의 `건물` 칸이 트레이에 반영되지 않는다** — 트레이(`buildTray` 4201줄)가 판 로드(8659줄)보다 먼저 만들어져, 목록 밖 물건의 카드가 그대로 남는다(놓기는 `canPlace` 훅이 막는다). README 는 "목록 밖은 트레이에서 빠진다"고 적고 있다 | 컨셉 스킨 구현 중(#734) | **기존 판 영향 0** 조건 때문에 이번엔 안 건드렸다(고치면 town·jobs-short 트레이가 달라진다) | 수업 판에서 '안 쓸 물건이 보인다'가 실제로 걸림돌이 되면 |

## 이미 꺼내 고친 것

- ~~아이가 누른 말이 자동 소식에 덮인다~~ → #720 · #722 · #724
- ~~다 자란 마을에서 길이 끊기면 `none` 으로 말한다~~ → #729
- ~~"길을 한 줄 더 내 봐요"를 따르면 마을이 나빠진다~~ → #728 (아이가 **틀린 것을 배우는** 경우라 즉시 고침)
- ~~붐빔이 집 4채·16명에서 시작한다~~ → #730(제안서) · #732(구현)
- ~~판 전용 종류가 든 저장본을 기본 마을로 가져올 때 조용히 버려진다~~ → #734 (컨셉 스킨과 함께 — "이 마을에 없는 것이 있어요(…)" 한마디)
