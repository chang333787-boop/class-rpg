# 2026-07-13 (맥북) — 울트라코드 퀘스트·보상 감사 + 1~3차 수정

## 감사 (read-only, 워크플로우 2개 · 총 22 에이전트 · 실패 0)
- **퀘스트 전 경로** (13 에이전트: 6축 탐색 → 축별 적대적 검증 → 종합) → `docs/rpg_quest_audit_20260713.md`
  - 원시 67건 → 중복 제거 후 **38건 확정**(NEW 30 · 🔴5 · 🟠15 · 🟡18), **반박 10건** 별도 기록
- **보상 전 경로** (9 에이전트: 4축) → `docs/rpg_reward_audit_20260713.md`
  - **26건 확정**(🔴3 · 🟠11 · 🟡12), 반박 7건
- 두 감사가 **독립적으로 A1(중복 지급)·A3(statVal 불일치)를 동일 지목** — 교차 검증됨.

## 수정 (전부 머지 완료)

### PR #123 `7ccacbd` — A1 · A2 · A3
- **A1 보상 2배 지급**: `approveReward` 진입부에 완료 가드(같은 boardQuestId 로그 있으면 보상 없이 신청만 정리) + `completeQuestForStudent`가 해당 pending 제거 + 퀘스트 관리 목록에 **⏳ 신청중** 표시
- **A2 주간퀘스트 잠김**: questLog `date`를 승인일 → **신청일**(`reward.date`), 승인 시각은 `approvedAt` 신설
- **A3 statVal 불일치**: kiosk 신청이 항상 1로 저장하던 것 + `completeQuestForStudent` +1 하드코딩 → 교사 설정값(소수 지원). questLog에 stat/statVal/boardQuestType 추가. 퀘스트 카드 '+1' 표시 수정
- 검증: 시뮬 12/12(중복 차단·정상 지급 보존·주간 잠김 해소·**daily 회귀 없음**·소수 보존)

### PR #125 `55b6bfa` — R3 · R7
- **R3**: `[보상 지급]` 탭이 능력치를 `parseInt`로 읽어 **골드만 줘도 소수점 절삭**(3.5→3)하던 것 → `parseFloat` + 소수1자리 반올림
- **R7**: `totalGold`를 갱신하는 곳이 승급 1곳뿐이라 **교사 승인 골드가 누적에서 전부 빠짐** → 지급 5경로(approveReward·completeQuestForStudent·applyReward·quickGiveGold·confirmBookRecord)에 반영. **차감은 제외**
- ⚠️ **과거 누락분은 소급되지 않음**(백필 = 운영 데이터 작업, 미포함)
- 검증: 시뮬 11/11

### PR #126 `3dbc77e` — R4 · R5 · B8 · B5
- **R4**: `approveReward`의 book 분기가 3필드만 저장해 **별점·분류·인물·줄거리 소실 + 별점/인물 업적 영구 미달성** → 독서 탭과 동일 필드 집합. `teacherChecked:false` 유지(교사 확인 흐름 보존)
- **R5**: `books[]`에 id가 없어 **교사 코멘트가 엉뚱한 책에 저장** → 두 저장 경로에 id 부여
- **B8**: 교사 능력치 기록 `statDefs`에 `life` 누락 → 🏠생활 퀘스트 기록이 통째로 안 보이던 것
- **B5**: '오늘만' 필터가 ⚠️확인 필요 보상까지 걸러 **뱃지 3건 vs 빈 목록** → 확인 필요는 필터 무관 표시
- 검증: 시뮬 13/13

**공통**: node-check 4종 · verify 18/1/0 · smoke 30/0/0 · **student.js 무변경**(학교 lane 비충돌).
캐시버스터: admin `?v=20260713c` · kiosk `?v=20260713` (student는 학교의 `20260713e` 유지).

## 🏫 학교 lane으로 넘기는 것 (student.js — 맥북은 안 건드림)
감사 보고서의 항목 번호로 표기. 상세는 두 감사 문서 참조.
- **R2 (🔴) 금요일 보스 무제한** — `student.js:3549~3586`. 반복 클릭으로 골드+**EXP 무제한**(전투는 하루 3회·EXP 0 설계인데 보스만 예외). `bossClaimedDate` 같은 1일 1회 가드 필요. **단 `bossActive` 기본 false라 현재는 미발동** → 보스를 켤 계획이면 먼저 수정.
- **R8 (🟠) 보상 알림·레벨업 연출 부재** — 승인으로 exp/gold/level이 올라도 학생 화면에 토스트도 `triggerLevelUp`도 없음. 전투 EXP 0 설계라 **레벨업의 유일한 경로인데 연출이 한 번도 안 뜬다**. `onDataChange`에서 이전 값 스냅샷 비교 필요.
- **R9 / B15 (🟠) '🎁 받기 가능' 오표시** — `student.js:8449` `selfApplied`가 **쓰는 곳 0인데 읽기만** 해서 미승인 대기가 승인 색으로 표시. 같은 화면 홈 배너는 '승인 대기중'이라 자기모순. claim 모델 잔재(`claimRewards` 호출부 0) 정리와 함께.
- **B11 (🟠) 자정 롤오버** — 태블릿을 켜둔 채 자정을 넘기면 같은 게시글이 다시 ○로 돌아가 중복 완료 가능.
- **B3 (🟠) 키오스크 탭 복귀 시 재렌더 없음** — `switchKioskTab`이 display만 토글(kiosk.js지만 학교가 보기 편하면 함께).

## 남은 것 (맥북 lane · 다음 후보)
- **B6** 재게시([↑]) 가 daily는 date 미갱신으로 바로 다시 내려가고, special은 과거 완료자가 영구 잠김
- **B7** 자동 일일퀘스트 설정 변경이 당일 반영 안 되고 **끄는 수단이 UI에 없음**
- **B9** `addBoardQuest` 입력 검증 없음(음수 보상 저장·0은 기본값으로 치환·중복 이름)
- **B1** 일일/주간 탭 하단 능력치 컨트롤이 읽히기만 하고 미반영(커스텀 항목에서 능력치 0 지급)
- **B12** 능력치 퀘스트 게시 시 이름 중복 항목이 **무고지로** 빠짐
- **B13** 반려가 기록·알림 없이 pending만 삭제
- **B16** questLog 키에 랜덤 성분이 없어 전체 승인 시 기록 1건 덮어쓰기
- **묶음 ③ (고위험·안전모드)** 일일 리셋을 "학생 로그인 부수효과"에서 **명시적 트리거로 분리** → Q1·A5·C5·C16·B11이 한 번에 정리됨. 별도 승인 필요.
