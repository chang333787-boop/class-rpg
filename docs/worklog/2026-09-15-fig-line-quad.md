# 2026-09-15 (기기: 학교 Windows) — 꺾은선그래프·사각형·두 직선 그림 (FIG-LINE-QUAD-1)

> 담당: 학습 조수(`st`). 작전_20260915 학습 후보 ② (#246 worklog 후보 1, 보스: 사회 4-2 뒤).

- **변경**: `figures.js` 새 kind 3개(+89줄, 기존 kind 무변경) · `curriculum.js` 수학 4-2 문항 32개에 `fig` 필드만 추가(물음·보기·답 무변경) · 캐시버스터 `figures.js`·`curriculum.js` `20260915stg`.

## 재현 → 수정 → 재현 안 됨
- **재현**: 꺾은선그래프 단원 문항이 그래프 없이 `[교실 온도] 오전 9시 18 ℃, …` 글로만 자료를 줬다. 사각형 단원도 그림 없이 "평행사변형의 한 각이 70도…". `figures.js`에 선그래프·사각형·평행선 렌더러가 없었다.
- **수정**:
  | kind | 규격 | 그리는 것 |
  |---|---|---|
  | `line` | `{ labels, values, unit?, min?, max?, step? }` | 눈금·가로선·점·선분·가로 이름·단위. `min>0`이면 0과 최솟값 사이 **물결선** |
  | `quad` | `{ type, deg?, angles? }` | 사다리꼴(평행 화살표 한 쌍)·평행사변형(두 쌍)·마름모(같은 길이 눈금)·직사각형(직각 표시)·정사각형(직각+눈금). `deg`로 실제 각 크기대로 기울임, `angles`로 꼭짓점 각·`?` |
  | `lines` | `{ type, label? }` | 수직(직각 표시)·평행·평행선 사이 수선(길이 또는 `?`) |
- 붙인 문항: 꺾은선그래프 19개(교실 온도·강낭콩·몸무게(물결선)·아이스크림·물 온도·줄넘기(물결선)) · 사각형/수직·평행 13개.
- **재현 안 됨**: 32개 모두 그림이 나온다.

## 검증
- 32개 SVG: 빈 그림 0 · NaN/undefined 0 · XML 파싱 오류 0
- 꺾은선 19개: 점 높이가 값에 비례(기울기 일정, 소수 0.2 눈금 4문항은 픽셀 반올림 0.3% 이내) · 같은 값은 같은 높이 · 그림 틀 안 · 가로 이름 모두 표시
- 눈으로 확인: 32개 모음 캡처(평행사변형 115°는 왼쪽으로 기울고 70°는 오른쪽으로, 마름모는 화살표 빼고 눈금만 — 처음엔 화살표·눈금이 한 점에 겹쳐 뺐다, 물결선이 최솟값 글자와 겹쳐 0 아래로 옮겼다)
- 렌더 하네스(MATH-42 하네스, 운영 write 0): 수학 4-2 210/210 표시·그림 39(기존 7+새 32)·정답 210 통과·오답 210 거부
- `verify-safety` PASS 18 · REVIEW 1 · FAIL 0 / `smoke-test` PASS 28 · REVIEW 0 · FAIL 0
