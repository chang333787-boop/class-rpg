# 2026-09-20 (주말 09-19~20) — 맥북 · 마당 꾸미기 **프로그램** 담당(mc)

> 월요일 학교 세션이 이어받는 기록. 머지는 전부 주말 보스 세션이 했다. 버스터 코드 `mc`.
> 짝: 마당 꾸미기 **디자인** 담당(그림·장식 표·값). 나는 `student.js/css/html` 의 꾸미기 부분 + `scripts/unit/` 꾸미기 시험.

## 1. 어떻게 시험했나 (다음 사람도 이대로)

- **놀이판**: `node scripts/unit/deco-save-count/play.mjs <빈 포트>` — 없는 프로젝트 + `goOffline()` · 자동 입장 · 운영 통신 0.
  A/B 는 main 을 `git archive origin/main | tar -x -C <임시폴더>` 로 풀고 `REPO=<임시폴더> node …/play.mjs <다른 포트>` 로 나란히 띄운다.
- **실제 터치**: 헤드리스 크롬 + CDP `Input.dispatchTouchEvent`(노드 24 내장 WebSocket, 의존성 0). 1366×610(크롬북 실뷰포트)·375×667·667×375·1024×768.
  합성 `dispatchEvent` 는 **브라우저 입력 파이프라인을 안 탄다** — 핀치 끊김(#430)은 합성 이벤트로는 1년을 돌려도 안 잡혔을 버그다.
  ⚠️ 합성으로 흉내 낼 때는 **그 순간 화면에 있는 캔버스**에 보내라(`#if-topview canvas`). `#deco-canvas` id 는 내 집 창 안 작은 판과 **겹친다**(둘 다 같은 id).
- ⚠️ 헤드리스 `--dump-dom --virtual-time-budget` 에서는 **rAF 가 몇 초씩 늦고, ResizeObserver 는 처음 한 번만** 불린다 → rAF/RO 에만 기대는 코드는 하네스로 못 잰다(그래서 `_decoFit` 은 아는 자리에서 바로 부르고, `_ffRedrawSoon` 은 타이머다).
- ⚠️ `precheck` 는 **HEAD** 를 본다 — 커밋하기 전에 돌리면 buster-check 가 헛돈다(#478 에서 버스터를 빠뜨리고도 PASS 를 봤다). **커밋한 뒤** 돌릴 것.

## 2. 찾은 것 · 고친 것 (심각도순 · 전부 main 머지됨)

| # | PR | 무엇이 (재현: 놀이판, 크기) | 고친 방식 |
|---|---|---|---|
| 1 | #430 DECO-PINCH-1 | 두 손가락 확대가 **첫 걸음(7%)에서 끊김** — 실제 터치로 3배 벌려도 1→1.07 (모든 터치 기기) | 확대 걸음마다 캔버스를 새로 만들던 것(`_dCv=null`) 제거 |
| 2 | #443 DECO-FIT-1 | 처음 열 때 캔버스 479 vs 보이는 327 → **마당 39~44줄이 서랍 밑**(1366×610·자리 기억 없는 기기) · 🖌️ 바닥 모드 **아래 30% 까맣게** · 최근 줄/서랍 펼침/창 크기 | `_decoFit()` — 서랍 다시 그림·모드 바꿈·서랍 펼침에서 바로 + ResizeObserver |
| 3 | #450 DECO-SHORT-1 | 1366×610: 카드 고르면 확대 기둥이 **'집 안으로' 단추를 덮음** · 마당이 화면의 39% · 폰은 서랍 펼치면 '바닥' 단추가 통째로 덮임 | 기둥 wrap-reverse + max-height · 키 작은 넓은 화면은 서랍 한 줄 · 하네스 '정말 눌리나'를 다섯 점으로 |
| 4 | #454 DECO-ANIM-HIT-1 | 🧽 치우기로 **보이는 강아지를 눌러도 "치울 게 없어요"** · 빈 풀밭(놓았던 칸)을 누르면 **멀리 있는 강아지가 사라짐** · 스포이드도 안 잡힘 | `_decoTopAt(…, seen)` — 보이는 동물 먼저, 떠나 있는 동물의 빈 자리는 건너뜀 |
| 5 | #465 DECO-FRIEND-PAN-1 | 내 마당을 옮겨 본 뒤 친구 구경 → **친구 마당 왼쪽 24% 검정·연못·동물 사라짐**(사회시뮬 세션 의심 → 보스 코드 근거 → 재현) | 구경 그리는 동안 pan 0·zoom 1·공간 1, 끝나면 되돌림(전체화면·방문 창 미리보기 둘 다) |
| 6 | #476 DECO-FRIEND-ART-1 | **처음 가 보는** 친구 마당이 옛 캔버스 그림(_DFN)·단색 물로 나오고 안 바뀜 | 구경 중 그림이 도착하면 구경 판도 다시 그림(`_ffRedrawSoon`, 0.05초 묶음) |
| 7 | #478 DECO-PHONE-INDOOR-1 | 폰 375 에서 집 안이 **칸 7px** — 가구·나가기 문을 못 누름 | `_decoPhoneStart` 를 집 안에도 |
| 8 | (이 PR) DECO-HARNESS-PRECHECK-1 | 하네스가 precheck 에 없음 · **가짜 통과**(동시 실행 시 못 뜨고 죽는데 `=false` 0 으로 읽힘) · 맥에서는 매번 타임아웃(120초)까지 기다림 · `구경뒤_내자리_그대로` 3~4회에 1회 false | 아래 §3 |

## 3. 하네스 (이 PR)

- **가짜 통과 셋을 막았다**
  ① 프로필 폴더 고정 → 동시 실행 시 `SingletonLock` 으로 못 뜸 → `mkdtemp`(끝나면 지움).
  ② 출력이 없어도 `=false` 가 없으니 통과로 읽힘 → **끝 표식(`걸린시간_초=`)이 없으면 exit 1**.
  ③ (찾다가 나온 것) 맥 크롬은 검사가 끝나도 **스스로 안 끝난다** — main 의 `run.mjs` 는 매번 노드 타임아웃(120초)에 죽인 뒤에야 `--dump-dom` 출력을 받았다(늘 2분·가끔은 못 받음). → 페이지가 끝나는 순간 **결과를 이 서버로 POST**(`/__done`)하고 바로 브라우저를 끈다. **한 번 120초+ → 3.5초.**
  덤: 바깥 스크립트(Firebase SDK 2·Chart.js)는 한 번만 받아 기기 임시 폴더(`rpg_harness_ext_cache`)에서 내준다(새 프로필이라 브라우저 캐시가 없다 · 못 받으면 원래 주소 그대로).
- `check.mjs`(새): 틀린 줄(`=false`·`"맞나":false`·`ERR`·`안눌리는단추_*≠"없음"`·출력 짧음)을 세어 exit 코드로. 틀리면 한 번 더 — 두 번 다 틀리면 FAIL, 두 번째만 맞으면 **REVIEW(흔들림 — 줄 이름이 찍힌다)**. 브라우저 없으면 SKIP.
- `precheck.mjs`: `deco-harness` — **base 대비 꾸미기 파일을 고친 PR 이면 저절로**, `--deco` 강제 · `--no-deco` 끔.
- `deco-save-real-sdk/run.mjs`: 고유 프로필 + 맥 크롬 지원(전엔 엣지 경로 고정이라 맥에서 못 돌았다). ⚠️ 이쪽은 아직 `--dump-dom` 방식이라 맥에서는 케이스마다 타임아웃까지 기다린다 — 같은 POST 방식으로 바꾸면 된다(다음 사람 몫). `teacherEdit` 는 지금도 🔴 `GOLD_REVERTED`(#288 그대로 — 관찰만).
- **`구경뒤_내자리_그대로` 흔들림 — 검사 쪽 문제로 판단(솔직히: 현장을 잡지는 못했다)**
  · 내가 본 것: 같은 증상 1회(폰 집 안 브랜치에서 4회 중 1회) + 같은 계열로 보이는 `핀치중_캔버스그대로=false` 1회.
  · 추정 원인: `openInteriorFullscreen()` 은 rAF 두 번 뒤에 `renderHouseDeco()+_decoViewRestore()` 를 부른다. 헤드리스 가상 시간에서는 rAF 가 몇 초씩 늦는다(직접 쟀다 — 3초 넘게 안 돌았다). 그 콜백이 **다음 시험의 비교 사이 0.1초**에 끼어들면 배율·자리가 바뀐다.
  · 계측판(끼어든 함수 이름을 적게 한 옛 검사)을 28회 돌렸지만 **재현이 안 돼 현장은 못 잡았다.** 그래서 추정에 기대지 않게 고쳤다 — ⑭ 는 그린 **직후 같은 틱**에 비교한다(자바스크립트는 한 줄기라 그 사이엔 아무것도 못 끼어든다). **이제 이 줄이 false 면 `_renderFriendCanvas` 자체가 내 자리를 바꾼 것 = 진짜 버그다.** 연 뒤에는 `rafSettle()` 로 그 rAF 가 실제로 돌 때까지 기다린다.
  · 결과: **40회(순차 20 + 4개 동시 × 5판) 전부 107줄 · 틀린 줄 0 · 103초.**
  · 실제 아이 화면: 실제 브라우저는 연 직후 2프레임 안에 그 rAF 가 돈다 → 구경 갔다 와서 자리가 틀어지는 길은 못 찾았다. 다만 **닫힌 뒤에 그 콜백이 도는 경우**(탭이 가려져 rAF 가 멈춘 사이 닫기)는 막혀 있지 않다 — `if (!_ifMode) return;` 한 줄. 다음 student.js PR 에 넣을 것.

## 4. 아직 안 고친 것 (플레이 시험에서 본 것 · 심각도순)

1. **동물이 키 큰 장식 앞에 보임**(벚나무 뒤 강아지가 나무 위에 그려짐) — 동물 층이 캔버스 위. 안: 동물 주변의 '발밑 줄이 더 아래인' 장식만 같은 DOM 층에 `<img>` 로 한 번 더 얹고 `z-index=발밑 줄`(동물은 이미 `z-index=줄`). 캔버스 그림과 같은 자리라 겹쳐 보이는 변화는 0. 움직임 층(`_mv.svg`)과 같은 층을 쓰게 될 테니 **그 작업과 한 번에** 하는 게 낫다.
2. **알림(toast)이 서로 겹쳐 글자가 뭉개짐** + 서랍 카드 둘째 줄을 2.4초 가림 — `toast()` 는 공용(내 구역 밖). 안: 꾸미기 전체화면일 때만 위치를 서랍 위로, 새 알림이 오면 앞 것을 바로 치움.
3. **폰 바닥 고르기 줄이 4줄(146px)** 로 접힘 → 폰 바닥 모드에서 판 위 끝이 200px. 바닥이 20종 가까이 되면 더 심해진다 — 디자인 담당의 '묶음 + 색 점' 시안 때 한 줄 가로 밀기로.
4. 폰 윗줄: '🧽 치우기'·'집 안으로'가 **화면 밖**(옆으로 미는 줄인데 표시가 없다). '← 내 기록으로'를 폰에서 '←' 로 줄이면 다 들어온다.
5. 끌어 놓기 알림 개수가 첫 칸을 안 셈(2개 놓고 "1개 놓았어요") — 터치는 누르는 순간 첫 칸이 따로 놓여서.
6. 동물이 **놓자마자 첫 걸음에 줄을 바꿈**(`lastHop` 0 시작 → "20~35초에 한 번"이 처음엔 안 지켜짐).
7. 집 안을 열 때도 "🌿 마당이 넓어졌어요" 안내가 뜸(처음 한 번).
8. 방문 창 안 작은 미리보기는 #476 의 다시 그리기가 안 붙어 있다(전체화면으로 들어가면 새 그림).
9. 후보였던 '문서·주석의 장식 수 표기': 내 구역에는 틀린 표기가 없었다 — `student.css` 주석의 "79종 중 4개"는 그날의 실측 기록이라 그대로 둠. **지금 실제: 전체 91 · 상점에 나오는 것 82 · hidden 6 · 마당 74/집 안 17 · 우리 4 · 먹이통 1 · `assets/deco/*.svg` 123장**(09-19 밤 main).
10. 관찰만(손대지 않음): 골드 유실 #288 — 꾸미는 중 교사가 골드를 주면 통짜 저장이 되돌림. 마당 안 상점이 들어오면 창이 더 중요해진다.

## 5. 보스 지시 대기열 (받은 순서 아님 — 보스가 정한 우선순위)

1. ~~하네스 흔들림 원인 + 가짜 통과 막기~~ → 이 PR
2. **지우기**: 마우스 오른쪽 **클릭**(안 끌고 뗌·4px 미만) = 그 자리 위의 것 치우기(오른쪽 끌기는 지금처럼 이동) · 터치 길게 = **작은 메뉴**[같은 것 집기·옮기기·뒤집기·치우기] · 2칸 이상·epic/legend·우리는 **끌어 치우기에서 건너뜀** · 알림 "🎒 … 가방으로 돌아갔어요 · ↩" · 하네스에 '어떤 치우기에서도 골드 불변' 한 줄.
3. **마당 안 상점 DECO-SHOP-1**: 서랍 [내 것 | 🛒 상점] · `buyDeco()` **그대로 재사용** · 구매 직전 `decoFlush()` · 골드는 구매 즉시 저장 · 위 막대에 보유 골드 · `deco-save-real-sdk/run.mjs teacherEdit` 전후(더 나빠지지 않았음). 시안: `보고_20260919/맥디자인_상점카드_홈타일_시안_1366x610.png`.
4. **홈에서 '우리 마을'과 나란히 + 이름 '마당 꾸미기'**(문자열 상수 한 곳) — 1366×610 실렌더로 '오늘의 공부' 카드가 안 밀리는지 재고.
5. 새 그림 연결: 정원 바닥(tulipbed 6색·hydrangea 4색·wildflower·특별한 색 — 한 파일 `#색` → **비트맵 캐시 키에 색**) · 꽃밭 가장자리 16조각(물가 `shore_*` 판정 재사용·바깥 변에만) · 밭 그림 5장(농장 1단계와) · 움직임 층(`motion.json`).
6. 놓는 방식 1단계(`place`·`on` 필드 → `_decoRuleWhy`, 필드 없으면 지금과 100% 같게 · 카드를 들면 놓을 수 있는 칸이 은은히) · 서랍 기능별 탭 · 좌우 뒤집기(`flip:1`).
7. **성능 재기 스크립트**(보스 방침 2 — 원래 맨 앞이었는데 버그가 먼저 나와 밀렸다): 크롬북 가정(CPU 4~6배 감속·1366×610)으로 로그인/홈/꾸미기 첫 그림/장식 200+바닥 절반에서 끌기·핀치 한 프레임/문서 크기/요청 수·바이트. CDP 도구가 이미 있으니(`Emulation.setCPUThrottlingRate`·`Network.*`) `scripts/unit/deco-perf/` 로 남길 것.
- 규칙: **student.js 를 고치는 PR 은 한 번에 하나**(버스터 줄이 늘 부딪힌다 — 주말에 다섯 번 부딪혔다). 협업자(사회시뮬)에게 직접 묻지 말고 보스에게 ⓐ있는 것 ⓑ방금 넣은 것 ⓒ보이는 문제 ⓓ후보.
- 굳은 규칙(협업자 조언 → 보스 채택): **마당 점수·순위 금지 / 동물 돌봄 의무 금지 / 움직임 예산.** 마당 꾸미기에 시뮬 규칙(필요·목표·해금)을 들이지 않는다.

## 6. 한 줄 제안 (구현 전 — 아이 입장에서 왜 좋은지)

- **구경 화면 확대·이동 + 동물 누르면 반응**(읽기 전용·저장 0): "○○네 강아지를 눌렀더니 왈! 하고 다가온다" — 남의 마당이 '작은 그림'에서 '가 본 곳'이 된다. (보스 채택 · 구경 전용 pan/zoom 상태를 따로 두면 #465 의 임시 0 처리도 자연스럽게 사라진다.)
- **놓는 순간 살짝 튀는 연출**: 놓인 장식이 0.2초 통 하고 내려앉음 — 터치에는 '눌렸다'는 손맛이 없어서, 놓였는지 알림 글을 읽어야 안다. (CSS 층 한 장·저장 0·움직임 줄이기 설정이면 끔.)
- **마당 사진 찍기**: 판 전체를 한 장으로(`canvas.toBlob` — 동물 층은 그 순간 자리로 합성). 자랑할 거리가 생긴다. 저장은 기기에만.
- **다 쓴 카드 → "🪙 하나 더"**(상점 PR 에 포함 예정): 지금은 다 쓰면 흐려지기만 해서 '왜 안 놓이지?'에서 멈춘다.

## 7. 테두리 울타리·길가 물건 — 프로그램 쪽에서 걸리는 점 (설계 메모만, 구현 안 함)

- **그리기 순서**: 지금은 발밑 줄(`_decoFootRow`) 오름차순 한 번. 테두리는 '칸의 위 변'과 '아래 변'이 앞뒤가 다르다 — 위 변은 그 줄 물건 **뒤**, 아래 변은 **앞**에 그려야 한다(좌·우 변은 그 줄과 같은 깊이). 한 줄을 [위 변 → 물건 → 아래 변]으로 세 번 나눠 그려야 하고, 동물 DOM 층도 같은 규칙으로 `z-index` 를 0.5 단위로 써야 한다.
- **동물 우리 판정**: 지금 `_penAt` 은 '우리 장식의 footprint 안쪽'이다. 테두리로 두른 닫힌 영역을 우리로 보려면 **닫힘 판정(flood fill)** 이 필요하다 — 장식을 놓고 치울 때만 다시 계산해 캐시(걸음마다 계산 금지). 열린 울타리는 우리가 아니다 → 아이가 한 칸 틈을 남기면 동물이 새는 게 '말이 되는' 동작인지 결정 필요.
- **되돌리기**: 네모로 끌어 두른 것은 한 단계(`stroke`)로 — 지금 틀 그대로 된다. 다만 기록 비교가 `id·area·row·col·sp` 라서 **변 필드(`e`)를 비교에 넣어야** 같은 칸의 다른 변을 잘못 지우지 않는다(`_decoUndoOne`).
- **옛 캐시 JS**: `e` 가 붙은 항목을 옛 JS 는 **칸 장식으로** 그리고, 그 칸을 '찼다'고 본다. 더 나쁜 것: 옛 JS 가 그 상태로 통짜 저장을 해도 필드는 보존되지만, 옛 화면에서 아이가 그 '칸 장식'을 눌러 치우면 테두리가 사라진다. → 테두리 항목은 **다른 목록(`houseEdges`)에** 두는 쪽이 안전하다(옛 JS 는 모르는 필드를 그대로 되저장한다 — 통짜 set 이라 CUR 에 실려 있으면 보존됨. 단 `_normalizeArrays` 가 모르는 배열을 어떻게 다루는지 먼저 확인).
- 가진 개수: 지금은 '놓인 수'를 `houseDecorations` 에서 센다 — 다른 목록에 두면 **세는 곳 5군데**(`_decoPlace`·`_decoStrokeApply`·`_decoPickAt`·`_decoUndoOne`·`renderDecoInv`/`_decoRenderQuick`)를 한 함수로 모으는 정리가 먼저다.

## 8. 열린 PR · 다음 할 일 (09-20 갱신)

- #545(하네스 precheck 편입·가짜 통과 셋·120초→3.5초) **머지됨.** 이제 꾸미기 파일을 고친 PR 은 `precheck` 가 하네스를 저절로 돌린다(약 4초).
- **열린 PR: #549 DECO-RCLICK-1** — 마우스 오른쪽 클릭 = 치우기 · 알림 "🎒 … 가방으로 돌아갔어요 (↩)" · 치우기 세 길을 `_decoRemoveOne` 한 곳으로 · `_ifMode` 가드. (§5-2 '지우기'의 첫 조각)
- 지우기의 남은 두 조각(각각 작은 PR, #549 머지 뒤):
  ① 🧽 모드 **끌어서 연달아 치우기** — 1×1·common/rare 만 끌어 치워지고, **2칸 이상·epic/legend·우리(pen)·동물이 든 우리는 건너뜀**(스쳐도 안 사라짐 · 누르면 치워짐). 한 번 끈 것 = 되돌리기 한 단계(`stroke` 에 `remove` 기록을 담으면 `_decoUndoOne` 이 그대로 되살린다). 손짓 쪽은 `paint` 를 erase 모드에서도 만들면 된다(`_decoAttachGestures` 의 pointerdown 분기 — 지금은 erase 모드 빈손이라 `drag`(화면 이동)로 빠진다 → erase 모드 한 손가락은 치우기, 이동은 두 손가락·✋ 로 안내).
  ② 터치 **길게 누르기 = 작은 메뉴**[같은 것 집기 · 옮기기 · (뒤집기) · 치우기] — 지금 600ms 스포이드(`_decoPickAt`) 자리에. 단추 44px·손가락 **위쪽**에. '옮기기' = 치우고 그 카드를 손에(안 놓고 그만두면 가방에 남으니 잃는 것 0). '뒤집기'는 `flip:1` 이 들어온 뒤에 단추를 켠다.
- 보스가 정한 그다음 순서(09-20 낮): **① 물 칸 그리기 순서 한 줄**(`_drawFloorSVG`: 지금 바탕→`fringe_grass_*`→`shore_*` 를 **바탕→`shore_*`→`fringe_grass_*`** 로 — 그림 PR #503 과 같은 날 머지) → ② 정원 바닥 연결(본보기 마당 #517 의 연결 규칙 · 한 파일 `#색` → 비트맵 캐시 키에 색) → ③ 마당 안 상점 → ④ 홈에서 마을과 나란히 + 이름 '마당 꾸미기'.
- 규칙 다시: student.js PR 은 **한 번에 하나** · 머지된 브랜치에 이어 커밋 금지(늘 `origin/main` 에서 새 브랜치) · `git reset --soft origin/main` 금지(그사이 main 이 움직이면 남의 새 파일이 '삭제'로 잡힌다 — 한 번 당했다. 커밋을 합칠 땐 `git reset --soft HEAD~N`).
- 도구: 실제 터치·실제 마우스 CDP 스크립트는 세션 임시 폴더에만 있다(터치 흉내를 켜면 `Input.dispatchMouseEvent` 가 안 먹는다 — 마우스 시험은 끄고). 성능 재기(§5-7)를 할 때 `scripts/unit/deco-perf/` 로 옮겨 남길 것.

## 9. 09-20 저녁 갱신 — 열린 PR 둘 · 다음 일을 시작할 자리

- 머지됨(추가): #549 오른쪽 클릭 치우기 · #550 worklog.
- **열린 PR**: **#561 DECO-WATER-ORDER-1**(student.js — 물 칸은 물가 먼저·잔디 번짐 나중 · **#503 과 같은 날 머지**) · **#557 DECO-PERF-METER-1**(scripts 만 — 성능 재기 + main 기준선).
- 성능 잣대: `node scripts/unit/deco-perf/run.mjs --runs 3`(전/후는 `REPO=<푼 폴더>` 로 **ABAB**). main 기준선(1366×610·CPU 4배): 로그인 665ms · 홈 ~800ms · 꾸미기 첫 그림 ~600ms · 홈까지 112건 2.7MB · 꾸미기 열 때 81건(그림 75건 낱개) · 무거운 마당 끌기 한 프레임 100ms(체크무늬 칠 = 최악) · 장식 1개 ≈ 46.5B. PR 본문에 이 숫자로 전/후를 적는다.
- 🐞 재면서 찾은 것(아직 안 고침 · 한두 줄): **전체화면을 닫은 뒤에도 동물 걷기 타이머가 돈다.** `closeInteriorFullscreen()` 이 안 보이는 작은 판(`house-topview`)을 다시 그리고 `_drawDeco → _animSyncLayer` 가 거기에 동물 층을 새로 만든다. 고침 안: `_animSyncLayer` 에서 `host.offsetParent === null`(안 보임)이면 `_animStopLayer(hostId)` 하고 돌아가기. 확인은 deco-perf 의 `20번열닫_걷기타이머`(지금 4 → 전체화면이 열려 있을 때 2, 닫혀 있을 때 0 이어야).

### 다음 일 = 정원 바닥 연결 (규칙 원문: `docs/deco_showcase_yard_20260920.md` §1·§4) — 짚을 곳
1. **저장값 해석 한 곳**: `_floorParse(v) → { name, color, rim }`(`'tulipbed#red+picket'` · 옛 값은 `{name:v}`). 지금 바닥 값을 읽는 곳 = `_drawYard` 의 `_floorCells`·`_yardTypeAt` · `_drawFloorSVG` · `_groundAt`(동물 바닥 묶음 — 꽃밭은 `soft` 로) · `_decoRuleWhy`(물 판정은 `=== 'water'` 라 그대로) · `_paintFloor`/`_decoStrokeApply`(같은 값이면 지우기 — **색·마감까지 같을 때만**).
2. **그림 이름·변형**: `_FLOOR_VARIANTS` 에 tulipbed 2 · tulipcol 2 · hydrangea 3 · wildflower 4 · sunflowerbed 2 · lavender 2 · daisyfield 3. 파일은 `tile_<이름>_<변형>.svg#<색>` — `_floorImg(name)` 의 캐시 키와 **`_floorBmp` 키(`'f:'+name`)에 색을 넣어야** 한다(같은 파일·다른 색 = 다른 비트맵). `<img src="…svg#red">` 의 `#색` 은 SVG 안 `:target` 방식이라 **이미지마다 따로 로드**된다(색 수만큼 Image 객체).
3. **가장자리**: 물가 블록을 그대로 본뜬 `bed_*`(마감 없을 때, `#색`)/`rim_<마감>_*`(색 없음). '꽃밭 무리'끼리 맞닿은 변은 안 그린다 · `wildflower` 는 풀(`_floorIsGrass` 에 추가). 순서 = 바탕 → 가장자리/마감 → 물가 → 풀 번짐(#561 이 뒤 둘을 이미 맞췄다).
4. **옛 캐시 JS 가 새 값을 만나면**: `FLOOR_TILES[값]` 이 없어 잔디 색으로 그린다(깨지지 않음) · 통째 저장이라 값은 보존된다. 단 옛 JS 에서 그 칸을 같은 도구로 칠하면 덮어쓴다 — 받아들일 만함.
5. **바닥 고르기 화면**: 지금도 폰에서 4줄(146px). 종류가 20 을 넘으니 디자인 담당 시안('흙·돌 / 풀·꽃 / 물' 묶음 + 꽃은 종류 1칸 + 색 점 + 마감)대로 **한 줄 가로 밀기**로 다시 짜야 한다. `student.html` 의 `#if-floor-row` 단추 14개가 인라인이라, 목록을 JS 표(`FLOOR_PICKER`)에서 그리게 바꾸는 '정리' 커밋이 먼저다.
6. 성능: 가장자리 조각이 늘면 칸당 drawImage 가 더 는다 → deco-perf 의 끌기 한 프레임을 전/후로. 나빠지면 '칸 묶음 굽기'(이웃 서명별 한 장)를 같이.
- 그 뒤 순서(보스): 마당 안 상점(`buyDeco()` 재사용·구매 즉시 저장·`teacherEdit` 전후) → 홈에서 마을과 나란히 + 이름 '마당 꾸미기' → 지우기 남은 두 조각(§8) → 놓는 방식 1단계 → 농장 1단계 → 서랍 탭·뒤집기·움직임 층.

## 10. 09-20 밤 — 멈춘 자리 (새 세션은 여기부터)

- 머지됨(추가): #557 성능 재기 · #561 물 칸 그리기 순서(+#503 새 물가 그림) · #565 하네스 안 rAF→타이머(핀치·구경 검사 흔들림의 길을 없앰 — **이 뒤에 같은 줄이 false 면 제품 버그로 읽는다**) · #563.
- **열린 PR: DECO-ANIM-HIDDEN-1 하나**(student.js · 버스터 mcj) — §9 의 🐞 '닫은 뒤에도 도는 동물 걷기 타이머'. `_animSyncLayer` 가 **안 보이는 판**(`getClientRects().length === 0`)에는 층을 만들지 않는다. 하네스 ⑩-4 · deco-perf `20번열닫_*` 로 전/후.
- 다음 = **정원 바닥 연결**, §9 '짚을 곳' 순서대로 작은 PR 넷: ① 저장값 해석 `_floorParse`(동작 불변 정리 — 옛 값만 있는 지금은 출력이 픽셀까지 같아야 한다) → ② 변형 표·이미지/비트맵 캐시 키에 색 → ③ `bed_*`/`rim_*` 가장자리(물가 판정 재사용 · 순서 = 바탕 → 가장자리/마감 → 물가 → 풀 번짐) → ④ 바닥 고르기 묶음 화면(시안 `보고_20260919/맥디자인_바닥고르기_묶음시안_1366x610.png`). 각 PR 에 본보기 마당(`docs/deco_showcase_yard_20260920.md`)과 같은 구도 전/후 + deco-perf ABAB.
- 그 뒤: 지우기 남은 두 조각(§8) → 마당 안 상점 → 홈 나란히 + 이름 '마당 꾸미기' → 움직임 층·밭 그림+농장 1단계 → 놓는 방식 1단계·서랍 탭·뒤집기. 마을 iframe `&v=` 는 #214 때(자리: `student.js` 의 `'village/index.html?sid=' + sid`).
- 주말에 굳은 작업 습관: **A/B 는 늘 main 을 푼 폴더와 나란히**(`REPO=`) · 터치/마우스는 **진짜 입력(CDP)** 으로 · precheck 는 **커밋한 뒤** · student.js PR 은 하나씩 · 공용 스택에 맨손 `git stash` 금지(한 번 쳤다가 SHA 로 되돌렸다) · 못 잡은 것은 못 잡았다고 적는다.

## 11. 09-20 밤 — 프로그램 담당 2대가 이어받음 · 정원 바닥 연결 ① (DECO-FLOOR-PARSE-1)

- **① 저장값 해석 한 곳** = 이 PR(student.js · 버스터 `mck`). `_floorParse(v) → { name, color, rim }` — `'#'`·`'+'` 없는 값은 이름 그대로, 빈 값·글자가 아닌 값·못 읽는 새 형식은 잔디, 색·마감은 `[a-z0-9_]` 만. 얼린 객체를 값별로 기억(상한 300)한다 — 칸마다·이웃마다 불려서.
- **§9 의 '짚을 곳'과 실제 코드가 달랐던 점**(코드가 진실):
  · 바닥 값을 '종류'로 읽는 곳은 **셋**뿐이었다 — `_floorCells`(칸 그리기)·`_yardTypeAt`(이웃 판정)·`_groundAt`(동물 바닥). `_drawFloorSVG` 는 값을 직접 안 읽고 위 둘이 넘겨 준 이름만 받는다 → ②에서 색을 받으려면 **인자를 하나 더** 넘겨야 한다(지금은 이름만).
  · `_groundAt` 은 해석 없이도 꽃밭을 `soft` 로 본다(물도 단단한 길도 아니면 전부 `soft`). 그래도 이름으로 읽게 했다 — 뒤에 `stone+…` 같은 값이 생겨도 묶음이 안 틀리게.
  · 지우개 판정 네 곳(`_paintFloor`·`_decoStrokeApply`·`_decoStrokeBegin` 두 줄)은 **저장값 글자 그대로** 비교한다 = 이미 '색·마감까지 같을 때만'. 고르기 화면이 늘 같은 순서(`이름#색+마감`)로 값을 만들면 손댈 것이 없다 → 안 건드렸다.
  · `_decoRuleWhy` 의 물 판정(`=== 'water'`)도 그대로(물에는 색·마감이 없다).
- **옛 마당 그림 불변을 재는 법(다음 PR 도 이대로)**: 하네스 ⑮ 가 옛 14종을 보이는 칸 전부에 깔고(연못 포함) 캔버스 전체 SHA-256 지문을 찍는다(`옛바닥마당_그림지문`). 지문은 기기마다 다르니 값 자체는 판정이 아니다 — **같은 기기에서 `REPO=<main 을 푼 폴더> node scripts/unit/deco-save-count/run.mjs` 와 나란히** 돌려 같은지 본다. 민감한지는 돌연변이로 확인했다(`gravel` 을 `gravel_yard` 로 읽게 바꾸면 지문이 바뀌고 `바닥해석_옛값_이름그대로=false`).
- 하네스 줄 수: 118 → **126**(⑮ 8줄). 단위 검사 171 → **182**.
- 그림: `docs/deco_floor_parse_20260920/`(놀이판 1366×610 · 옛 14종 마당 · 전/후 파일이 바이트까지 같다).
- 다음 = **② 변형 표 + 이미지/비트맵 캐시 키에 색**: `_FLOOR_VARIANTS` 에 정원 바닥 7종 · `_floorImg(name, color)` 가 `…svg#색` 을 부르고 기억 키를 `이름#색` 으로 · `_floorBmp` 키도 같은 키(`'f:'+키`) · `_drawFloorSVG` 에 색 인자 · `FLOOR_TILES`/`FLOOR_TILE_COLORS` 에 폴백 색(그림이 늦게 올 때 잔디색이 번쩍이지 않게). 고르기 화면은 ④까지 안 연다 — ②·③은 놀이판에서 값을 직접 넣어 본보기 마당과 같은 구도로 찍는다.

## 12. 09-20 밤 — 정원 바닥 연결 ② 변형 표 · 그림/비트맵 기억 키에 색 (DECO-FLOOR-COLOR-1)

- #602(①) 머지됨. **②** = 이 PR(student.js · 버스터 `mcl`). 고르기 화면은 아직 안 연다 → 아이 화면에 보이는 변화 0, 저장값에 정원 바닥이 있으면 **색까지 그려진다**까지.
- 넣은 것: `_FLOOR_VARIANTS` 에 정원 7종 · `_FLOOR_COLORS`(종류별로 **그림 파일에 실제로 있는 색** — 단위 검사가 파일 안 `#색:target` 과 맞춰 본다. 디자인 담당이 색을 더하면 이 표에 한 낱말) · `_floorImg(name, color)`(주소 끝 `#색` · 기억 키 `이름#색`) · `_drawFloorSVG(…, color)`(비트맵 키에도 색).
- **쓴 색만 부른다**: 색마다 Image 가 따로지만 그 칸을 그릴 때 처음 부른다. 옛 바닥만 깐 마당은 정원 그림 0장(하네스 `정원바닥_안쓴마당_부른그림0`) · deco-perf 꾸미기 열 때 요청 81건·450KB 그대로. 크롬은 같은 파일의 다른 `#색` 을 **한 번만 받는다**(최악 마당: Image 87개인데 바닥 요청은 파일 18개).
- 표에 없는 색(`tulipbed#nosuchcolor`)·기본색 이름(`tulipbed#pink`)·옛 바닥에 붙은 색(`stone#red`)은 **기본색으로 그리고 그림을 새로 안 부른다** — 저장본에 엉뚱한 색이 아무리 많아도 Image·비트맵은 표만큼(87)만.
- **최악 마당**(정원 7종 × 색 전부 = 36값을 3486칸에 돌려 깖 · 1366×610 · 배율 0.5↔3 을 62걸음으로 왕복): Image 87 · 비트맵 최대 **1026**(상한 1500 안 · 비워짐 0) · 비트맵 **37MB** · `_floorParse` 기억 37(상한 300) · 가장 긴 프레임(새 계단을 굽는 순간) CPU 4배에서 194ms(기기가 바쁠 땐 528ms) · 데운 뒤 11.5ms. 견줄 값 — 옛 14종 체크무늬 마당: Image 49 · 비트맵 527 · 21MB · 가장 긴 89ms · 데운 뒤 51ms(물가·잔디 번짐 조각이 많아서).
  → 버틴다. 다만 **옛 14종 최악 + 정원 최악 + 장식 다수를 한 마당에서 끝에서 끝까지 확대**하면 1500 을 넘어 한 번 통째로 비워질 수 있다(그 순간 한 프레임이 길어진다 · 깨지지는 않는다). 고칠 때가 오면 '통째 비우기' 대신 '지금 계단에서 먼 것부터 버리기'.
- 그림이 늦게 올 때의 폴백은 **잔디색 그대로** 뒀다(①의 보고에서는 폴백 색을 넣겠다고 했는데 바꿨다): `FLOOR_TILES` 는 '옛 14종 단색 표'라 거기에 넣으면 그 뜻이 흐려지고, 꽃밭은 바탕이 풀이라 첫 0.1초의 잔디색이 어색하지 않다.
- ⚠️ **못 본 것 — 사파리(아이패드)**: 한 파일·여러 `#색` 을 캔버스에 그릴 때 웹킷이 색을 제대로 가르는지 **확인하지 못했다**(이 기기에서 자동으로 몰 길이 없었다). 크롬(크롬북·안드로이드)은 확인. ④에서 고르기 화면을 열기 **전에** 아이패드 한 대로 본보기 장면을 봐야 한다. 안 되면 길은 하나 더 있다 — 파일을 한 번 받아 색 변수만 바꿔 넣은 blob 주소로 Image 를 만든다(요청 수도 같다).
- 놀이판 장면: `scripts/unit/deco-save-count/scene-garden.js` — 꾸미기 전체화면을 연 뒤 통째로 실행 → `__sceneGarden()`(본보기 마당 그림 1 과 같은 구도) · `__sceneGarden('worst')`(최악 마당). ③·④도 이 구도로 전/후를 찍는다.
- 그림: `docs/deco_floor_color_20260920/`(전 = main 에 같은 값을 넣은 것 → 잔디색 체크무늬 · 후 = 이 PR).
- 하네스 126 → **133줄**(⑯ 7줄) · 단위 검사 182 → **192**. ⑮ 의 '옛 14종'은 표 앞 14줄로 고정했다(표가 늘어도 지문 불변 · 지문은 main 과 같다 `fe4413ee…`).
- **④(고르기 화면)의 합격선으로 적어 둔다**: 저장값은 늘 **`이름#색+마감` 순서 · 기본색은 색 없이(`tulipbed`, `tulipbed#pink` 아님) · 마감 없으면 `+` 없이** 만든다. 지우개 판정 네 곳이 저장값을 글자 그대로 비교하기 때문이다(같은 바닥을 다시 칠하면 걷힌다). 값을 만드는 곳은 한 함수(`_floorJoin(name, color, rim)`)로 두고 `_floorParse` 와 왕복 검사를 붙일 것. 화면 규칙은 디자인 담당의 **(다) 견본 판 + [끌어서|네모로]**.
- 다음 = **③ 가장자리**: 꽃밭 무리(`wildflower` 빼고 6종)의 바깥 변에 `bed_*#색`(마감 없을 때)/`rim_<마감>_*`(색 없음) — 물가 판정을 그대로 본뜸 · 서로 다른 꽃밭이 맞닿은 변은 안 그림 · `wildflower` 를 `_floorIsGrass` 에 · 순서 = 바탕 → 가장자리/마감 → 물가 → 풀 번짐. 지금은 꽃밭 둘레에 **잔디 번짐(`fringe_grass_*`)이 그려진다**(꽃밭이 '잔디가 아닌 칸'이라서) — ③에서 꽃밭 무리는 잔디 번짐을 빼고 제 가장자리를 그리게 바꾼다. `bed_*` 의 색 표는 `_FLOOR_COLORS` 와 같은 이름인지 파일로 확인할 것.
  · (미리 본 것) `bed_*.svg` 의 색은 **blue · orange · pink · red · violet · white · yellow** 일곱뿐이다 — 바탕 그림의 색 이름(candy · sherbet · night · duo · moon · lemon · rainbow · snow…)과 **다르다.** ③에는 '바탕 색 → 떨어진 꽃잎 색' 짝 표가 필요하다(예 `tulipbed#candy` → ? · 기본색 `tulipbed` → pink · `hydrangea` → blue · `sunflowerbed` → yellow). 짝은 디자인 담당에게 물어 정한다(내가 눈대중으로 정하지 않는다).

## 13. 09-20 밤 — 정원 바닥 연결 ③ 꽃밭 가장자리 `bed_*` · 마감 `rim_*` (DECO-FLOOR-EDGE-1)

- #619(②) 머지됨. **③** = 이 PR(student.js · 버스터 `mcq`). 앞 담당이 커밋 전에 한도로 멈춘 워크트리를 이어받아 마무리했다(버린 것 없음). 고르기 화면은 아직 안 연다 → 저장값에 꽃밭이 있으면 **둘레가 잎·꽃잎으로 번지고, 마감이 있으면 테를 두른다**까지.
- 넣은 것:
  · `_drawFloorSVG` 의 열두 조각 판정(변 4 · 안 모서리 4 · 바깥 모서리 4)을 `ring(pre, 변형, 색, n,e,s,w, out)` 하나로 모았다 — 물가·꽃밭 가장자리·잔디 번짐이 **같은 판정·같은 순서**. 물가·잔디 번짐은 판정·그리는 순서가 전과 한 글자도 다르지 않다(옛 마당 지문이 증명).
  · **꽃밭 무리** = `_FLOOR_EDGE_COLOR` 표에 있는 6종(`wildflower` 없음). 이웃이 꽃밭 무리가 **아닌** 변에만: 마감 없으면 `bed_*#꽃잎색`, 마감 있으면 `rim_<마감>_*`(색 없음). 꽃밭 칸에는 **잔디 번짐을 안 얹는다**(둘레는 제 가장자리가 맡는다 — 전에는 꽃밭이 '잔디 아닌 칸'이라 풀이 번졌다).
  · 종류·색이 달라도 꽃밭끼리 맞닿은 변은 안 그린다(색을 번갈아 깔면 줄무늬 화단 하나 — `docs/deco_floor_edges_20260920/after_stripes_zoom2.jpg`).
  · ⚠️ **판단 하나(보스 확인 바람)**: 마감 있는 칸은 **같은 마감의 꽃밭만** 안쪽으로 친다 → 말뚝 밭이 마감 없는 밭·다른 마감 밭과 맞닿으면 그 변에도 말뚝이 선다(울타리가 늘 빙 둘러 닫힌다). 마감 없는 칸은 옆 밭의 마감과 상관없이 꽃밭이면 안쪽(꽃이 옆 울타리까지 닿는다). 본보기 스크립트는 마감 칸도 '꽃밭이면 안쪽'이었다 — 그렇게 되돌리려면 `out` 의 `return !!rm && …` 한 줄을 `return false` 로.
  · `_floorIsGrass` 에 `wildflower` — 들꽃 잔디 칸은 조각 0, 돌·물 칸은 들꽃 잔디 쪽으로 잔디가 번진다.
  · `_yardTypeAt(r, c, wantRim)` — 셋째 인자가 참이면 이웃의 마감을 준다(해석은 `_floorParse` 한 곳 그대로).
- **색 짝 표** = 디자인 담당의 `docs/deco_floor_edge_colors_20260920.md`(#636) 그대로 `_FLOOR_EDGE_COLOR` 에 채웠다(눈으로 고른 짝: candy→red · duo→violet · sherbet→pink · night→violet · moon→yellow · lemon→yellow). 표에 없는 새 바탕 색은 그 바닥의 `''` 짝 · `pink` 는 bed 그림의 기본색이라 주소에 안 붙인다(같은 그림을 두 번 안 부른다). 단위 검사가 '표가 `_FLOOR_COLORS` 를 빠짐없이 덮는다'를 본다 — 디자인 담당이 색을 더하면 이 검사가 짝을 채우라고 알려 준다.
- **옛 마당 불변**: 하네스 ⑮ `옛바닥마당_그림지문` = main 을 푼 폴더(`REPO=`)와 나란히 `9b8274e878dc863546c0` 같음 · `옛집안_그림지문` 도 같음. 옛 마당만 깐 곳에서 부른 `bed_`/`rim_` 그림 0(`정원바닥_안쓴마당_부른그림0` 을 넓혔다).
- **돌연변이**: '다른 꽃밭과 맞닿은 변은 안 그림'을 '이름이 다르면 바깥'으로 바꾸면 단위 2개 FAIL · 하네스 `꽃밭가장자리_다른꽃밭·다른색·다른마감과_맞닿으면_안그림=false` · 옛 마당 지문은 그대로(정원 바닥이 옛 마당에 새지 않음). 되돌린 뒤 전부 PASS.
- **성능**(1366×610 · CPU 4배 · 기기가 바빴다(load 11~39) → main 과 **동시에** 띄워 짝으로 쟀다):
  · 가장자리 최악 = 꽃밭·잔디 체크무늬(꽃밭 칸마다 8조각 · 6종 × 색 × 마감 전부 돌려 깖) 끌기 한 프레임: main 77.9/77.0/100.1/79.2/94.6/112.8 ↔ 이 PR 80.4/76.8/93.9/82.1/90.4/116.5ms — **차이 없음**(main 도 같은 칸에 잔디 번짐 8조각을 그렸다 · 칸당 최대 조각 수가 같다). 따로따로 번갈아 쟀을 때는 +20~30% 로 보였는데 동시 짝으로 재니 사라졌다 = 기기 흔들림.
  · 정원 최악(②의 36값 전부 · 대부분 꽃밭끼리 맞닿음) 2.2 → 2.9ms: 들꽃 잔디와 맞닿은 변에 **새로 그리는 가장자리** 몫(+0.7ms). 한 칸 조각 수 상한은 그대로.
  · deco-perf(옛 바닥 무거운 마당) 끌기 260.5 ↔ 259.7ms · 꾸미기 열 때 요청 81건·450KB 그대로.
  → '이웃 서명별 칸 묶음 굽기'는 **안 넣었다**(10% 기준에 안 걸림). 비트맵 상한 방식도 그대로.
  · Image·비트맵 수(가장자리 최악): Image 110 → 212 · 비트맵 123 → 225(상한 1500 안). bed 조각은 쓴 꽃잎 색만 부른다.
- 그림: `docs/deco_floor_edges_20260920/`(전 = main · 후 = 이 PR · 본보기 구도 기본 1366×610 + 가까이 배율 2.5 셋 + 줄무늬·맞닿은 밭 배율 2 둘). 눈으로 본 것: 해바라기·데이지·수국 둘레가 **잎이 풀 위로 번지고 꽃잎이 떨어진 모양**으로 읽힌다(네모 벽돌 마감 아님) · 벽돌·말뚝 마감은 칸 경계를 따라 곧은 테. 라벤더 벽돌 테는 배율 2.5 에서도 가늘다(디자인 담당 '남은 흠'과 같음).
- 하네스 179 → **194줄**(⑰ 15줄) · 단위 검사 192 → **209**(가장자리 17).
- 놀이판 장면: `__sceneGarden('edges')`(가장자리 최악) · `__sceneGarden('stripes')`(줄무늬·맞닿은 밭·들꽃 속 데이지·돌).
- ⚠️ 못 본 것: 사파리(아이패드)에서 `bed_*.svg#색` — ②와 같은 숙제(④ 전에 한 대로 볼 것). 실제 크롬북 기기에서의 프레임.
- 다음 = **④ 바닥 고르기 화면**: `_floorJoin(name, color, rim)` 한 함수로 저장값을 만들고 `_floorParse` 와 왕복 검사(기본색·마감 없음은 글자를 안 붙인다) · 디자인 담당 (다) 견본 판 + [끌어서|네모로] · 견본 칸도 이 PR 의 `_drawFloorSVG` 로 그리면 가장자리까지 보인다(견본용 `typeAt` 은 칸 밖을 잔디로 주면 홀로 선 밭 모양) · 열기 전에 아이패드 확인.

## 14. 09-20 밤 — 정원 바닥 연결 ④-1 바닥 고르기 화면 (DECO-FLOOR-PICK-1)

- #641(③) 머지됨 · 보스: 마감 판정은 지금대로('같은 마감의 꽃밭만 안쪽' — 울타리는 닫혀야 울타리). **④-1** = 이 PR(student.js·css·html · 버스터 `mcr` 둘 다). 규칙 원본 `docs/deco_floor_picker_20260920.md`. 칠하기는 지금의 **끌어서만** — [✏️ 끌어서 | ⬛ 네모로] 토글과 네모 손짓은 ④-2.
- 넣은 것:
  · 🖌️ 바닥 모드에서 위쪽 글자 단추 줄(`#if-floor-row`)을 **없앴고**, 접힌 서랍 자리에 `#if-floor-picker` — 견본 판(왼쪽) + 가족 칩 · 색 · 테두리(정원) / 기본 14바닥(기본 칩, 옛 단추 순서 그대로 · 좁으면 두 줄로 접힘).
  · **저장값 = `_floorJoin(name, color, rim)` 한 곳** — `이름#색+마감` 순서 · 기본색이면 `#` 없음 · 자연이면 `+` 없음 · 들꽃·옛 바닥엔 마감 없음 · 모르는 색/마감은 떨어진다. `_floorParse` 와 왕복(모든 조합)을 단위 검사가 본다 → 지우개 판정(글자 그대로 비교)이 그대로 맞는다.
  · 견본·칩·색 동그라미·테두리 그림은 **마당과 같은 `_drawFloorSVG`** 로 그린다(`_floorBoard` — `_dCtx` 를 잠깐 그 캔버스로 바꿔 그리고 되돌린다). 판 밖은 '경계 없음'이라 둘레를 보이려고 잔디 테를 판 안에 둔다 → 홀로 선 밭의 가장자리·마감까지 보인다. 그림이 늦게 오면 `_floorImg` onload 에서 판만 다시 칠한다(단추는 그대로 — 누르는 도중에 단추를 바꾸면 눌림이 사라진다).
  · 가족마다 마지막 색·테두리를 기억(이 창 안에서) · 바닥 모드를 열 때 지금 붓(`CUR_FLOOR_TILE`)을 거꾸로 읽어 칩·색·테두리를 맞춘다.
  · **잠긴 색**: 🔒 12px · 그림은 안 흐리게 · 누르면 칠하지 않고 말풍선(아이 말 두 줄) + 견본을 그 색으로 어둡게 + 🔒 · 2.5초 뒤(또는 판 밖을 누르면) 닫히고 고른 것은 그대로. 문턱 = `docs/deco_garden_family_20260920.md` 표(`_floorLockWhy`). 라벤더·해바라기·데이지는 표에 문턱이 없어 전부 열림.
  · ⚠️ **문턱은 원래 '가진 적 있음'(도감)인데 그 기록이 아직 없다** → 지금은 **지금 가진 것**(인벤토리 수 > 0)으로 본다. 팔면 다시 잠기지만 이미 칠한 칸은 그대로다. 도감 기록이 생기면 `_floorHas` 한 줄만 바꾼다.
  · **집 안 공용 조각**: `_pkCanvas`·`_pkChip`·`_pkDot`·`_pkTile`·`_pkSwatch`·`_pkBubble` + CSS `.pk-*` — 그림 그리는 함수(draw(ctx,w,h))만 넘기면 된다. IN-2 담당이 벽지·바닥 그림 함수만 바꿔 같은 말투로 쓸 수 있다(집 안 코드는 안 만졌다).
  · 기둥(＋－전체✋↩)은 `--deco-drawer-h` 를 바닥 모드에선 고르기 판 높이로 잰다(`_decoPillarSync`).
- **옛 마당 불변**: `옛바닥마당_그림지문` · `옛집안_그림지문` main 과 같음(`9b8274e8…` · `0849e38a…`). `정원바닥_안쓴마당_부른그림0` 은 '이 마당을 그리며 **새로** 부른 정원 그림 0'으로 좁혔다 — 이제 🖌️ 바닥 모드를 열면 고르기 판이 칩 그림으로 정원 그림을 부르기 때문(꾸미기를 **열 때** 요청은 그대로 — 바닥 모드를 눌러야 부른다).
- **돌연변이**: `_floorJoin` 이 `+마감` 을 `#색` 앞에 붙이게 바꾸면 단위 2 FAIL(왕복 · 표 글자) + 하네스 3줄 false(`바닥고르기_빨강튤립_흰말뚝` · `_가족마다_마지막것기억` · `_칠하면_그글자_다시누르면_걷힘`). 되돌린 뒤 PASS.
- 자리(놀이판 실측 · 마당 보이는 비율): 1366×610 정원 **58%** · 기본 61% / 1024×768 67% · 69% / 768×1024 76% · 76%. 누를 곳 44px 미만 0. 판 다시 그리기 한 번 3~5ms(감속 없음).
- 하네스 194 → **214줄**(⑰ 20줄) · 단위 209 → **217**.
- 그림: `docs/deco_floor_pick_20260920/`(전 = main 의 🖌️ 바닥 모드 · 후 = 정원·기본 세 폭 + 잠긴 달빛 수국).
- ⚠️ 못 본 것: 아이패드 사파리(보스 숙제 목록) · 실제 크롬북 터치로 칩 줄 옆으로 밀기(합성 클릭만 했다).
- 다음 = **④-2 '네모로'**: 도구 토글([✏️ 끌어서 | ⬛ 네모로] 높이 44 · 1200 이상은 칩 줄 맨 앞, 미만은 견본 아래) · 끄는 동안 반투명(.55) 미리보기 + 금색 점선 + 'N×M칸 · 손을 떼면 칠해져요 · 잘못하면 ↩' · 떼면 한꺼번에(집·밭 칸 건너뜀) · ↩ 한 번 = 네모 하나(`_decoUndoPush` 에 칸 묶음 한 기록) · 확인 창 없음 · 지우기 없음 · 두 손가락은 그대로. 캡션 첫 줄 '모서리에서 모서리로 끌어요'. 고른 도구는 기억.

## 15. 09-20 새벽 — 장식 전수조사 반영: 상점에서 9종 빼기 · 잠긴 색 문턱 (DECO-RETIRE-2)

- #650(④-1) 운영에 나감. ④-2 앞에 작은 PR 하나 — 사용자 지시 '장식 전수조사'(`보고_20260919/마당_축척감사/전수조사_98종.md`) 반영.
- **먼저 고친 것 — 잠긴 색 문턱**: `_floorKindCount` 가 '상점에 있는 것(!hidden) 중 가진 것'으로 세서, 장식을 상점에서 빼면 그걸 가진 아이의 색이 다시 잠겼다(연못 둘을 빼면 수국 보라·분홍·흰이 잠김). → **가진 것은 hidden 이어도 센다** · '전부'는 지금 상점에 있는 것 전부(뺀 것은 더 살 수 없으니 목표에서 뺀다). 단위 1건 + 돌연변이(옛 식으로 되돌리면 FAIL) 확인.
- **상점에서 빼기(`hidden:true` · 가진 것·놓은 것은 그대로)** 9종, 줄마다 `// [DECO-RETIRE-2] … 대신 X`: 큰 연못 d_y59 · 작은 연못 d_y31(→ 🖌 물 바닥+물가) · 큰 헛간 d_y66(→ 헛간 d_y28) · 장작더미 2 d_y37(→ d_y23) · 큰 바위 2 d_y38(→ d_y26) · 밀밭 B d_y35(→ d_y25) · 큰 화단 d_y16(→ 정원 바닥+벽돌 마감) · 과수원 d_y64(→ 과수나무 d_y48 넷) · 둥근 나무 2 d_y47(→ 작은 나무 d_y9). 동물 무리(오리 가족·닭 3마리·양)는 살림.
- **이름 겹침**: 황금 책장 d_i12 → '금빛 책장'(업적 장식 deco_bookshelf '황금 책장 (업적)'은 그대로 — 상점 이름은 '(업적)'을 떼서 둘이 같아 보였다).
- 버스터: gamedata `mab`→`mac`(student·admin·kiosk) · student.js `mcs`→`mct`.
- 놀이판: 9종을 가진·놓은 아이 마당 — 9개 다 그려짐 · 🎒 내 것에 9개 · 🛒 상점에 0 · `_decoShopState` 전부 'none' · 수국 잠김은 duo·moon 뿐(숨긴 연못도 '가진 것'). 그림 `docs/deco_retire_2_20260920/`.
- balance-identity REVIEW '상점 목록 달라짐' = 의도(9종 뺌 · 이름 1).
