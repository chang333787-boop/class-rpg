# 2026-06-18 작업 로그

## 2026-06-18 (기기: 학교 Windows)
- 한 일:
  - 학교 Windows PC를 rpg 작업 환경으로 셋업.
  - repo를 `C:\Users\USER\projects\class-rpg` 에 clone (OneDrive 밖).
  - 작업 진입점 `CLAUDE.md`(공유) + `CLAUDE.local.md`(이 PC 전용) + `.gitignore` + `docs/worklog/` 추가.
  - 두 기기 작업방식 정리: 안전규칙·지시문·보고형식은 공유 동일 / 감독 역할만 환경별
    (맥북 GPT 별도앱, Windows 내부 Codex).
  - Obsidian 설치(이 PC) — repo 폴더를 볼트로 사용 예정.
- 기준 commit / 브랜치: `main` (clone 시점 최신)
- 검증 결과: (코드 변경 없음 — 문서/설정만 추가)
- 결과: 위 파일들을 main에 commit+push 완료 (`19126e4`). 맥북은 `git pull`로 같은 규칙 수신.
- 다음 할 일:
  - **맥북**: 다음에 열면 `git pull --ff-only` → CLAUDE.md/worklog 받기 (규칙 동기화 확인).
  - 코드 작업 복귀 시 README/safety_rules 절차 그대로 (조사 먼저 · 작은 Phase · 보고 후 정지).
- 주의/미해결:
  - 이 PC엔 `gh` CLI 없음 → PR 필요 시 설치 또는 웹.
  - rpg를 양쪽에서 만질 땐 "앉으면 pull / 끝나면 push", 같은 브랜치 동시작업 금지.
