# 2026-07-30 (맥북) — 울트라코드 감사 후속 수정 + 실화면 점검 환경 구축

사용자 부재 중 자율 진행. **되돌리기 어려운 운영 데이터 조작은 하지 않음**(도구만 만들고 실행은 교사 버튼으로 남김).

## 🆕 실화면 점검 환경 (이번 세션 최대 변화)
- `~/.claude/launch.json`에 `class-rpg`(포트 8002) 등록 → 브라우저에서 **실제 화면·실데이터로 검증 가능**해짐.
- 이전까지 이 프로젝트의 검증은 정적 분석 + 로직 시뮬뿐이었다. 이제 콘솔 오류·DOM·실데이터 확인 가능.
- ⚠️ **주의**: `student.html`/`admin.html`은 **로드만 해도 운영 DB에 쓸 수 있다**(`ensureDailyQuests`/`autoCloseDailyQuests`).
  열기 전 반드시 확인: `settings.autoDailyLastDate === 오늘` 이고 `date!==today`인 활성 daily가 0이면 안전.
  `kiosk.html`은 로드 시 읽기만 하므로 항상 안전.

## 📊 실환경 측정으로 밝혀진 것 (코드만 봐선 못 찾던 것)
| 노드 | 크기 | 비중 |
|---|---|---|
| **backups** | **4,925KB** | **75%** |
| questLogs | 529KB | |
| **quests (파생 중복본)** | **269KB** | 삭제 가능 |
| emotionLogs | 220KB | |
| boardQuests (708개·활성 9) | 128KB | 2% |
| **전체 DB** | **6.4MB** | |

앱은 루트 전체를 `on('value')`로 구독 → **학생이 한 번 저장할 때마다 접속 중인 모든 기기가 6.4MB를 다시 받는다.**
7명×하루 50회 = 하루 2.2GB, 30명이면 하루 40GB+ (무료 티어 10GB/월). 감사 ER-4가 실측으로 확정됨.

기타 실데이터 확인:
- 중복 지급 위험 상황 **현재 0건**(A1 가드는 예방으로 대기)
- 집계 왜곡(C1) 원인인 고아 로그 **0건** → C1은 현재 무해
- `Utils`/`DB`는 `const`라 `window`에 없지만 스크립트 스코프 접근 정상 → A1 가드 런타임 안전 확인

## 머지된 PR
| PR | 내용 |
|---|---|
| **#131** `d07086f` | **B4** 키오스크 마감 배지 — daily 마감 15:00 하드코딩 제거(주석은 '오후 4시'로 불일치), `dueDate` 최우선 반영, daily는 자정 기준. **브라우저 전/후 확인**(`⌛마감됨` → `⏰오늘까지`) |
| **#132** `acd3fa0` | **ER-4/ER-3** 백업을 구독 루트 **밖**(`classRPG_backups`)으로. 읽기는 새+옛 병합이라 **배포 즉시 기존 백업 그대로 보임**. 백업 대상 8→**22개**(감정·추억·주간다짐·리코더·어휘·설정이 백업조차 안 되던 것 해소), 보관 7→14일. 롤백을 root 통짜 set → **노드별 set**으로(파생 quests 실저장·미백업 노드 덮어쓰기 제거). 교사용 **[📦 이전]** 버튼 추가 |
| **#133** `9bade97` | **파생 노드 정리 도구** — DB에 `quests` 958건(269KB)이 실제 저장돼 있음을 확인. `_normalizeArrays`가 항상 questLogs에서 재생성함을 **오염 데이터 주입으로 검증** → 삭제 안전. 교사용 **[🧹 정리]** 버튼 |
| **#134** `9d4f7c9` | **B2/C6** 키오스크 — 닫힌 퀘스트 신청 차단(태블릿과 동작 불일치 해소, **브라우저에서 차단 확인**), 저장 실패 시 낙관적 항목 롤백(재시도 막히던 것) |

앞선 세션 분(#123·#125·#126·#128·#129·#130)까지 합쳐 **누적 10 PR**.
공통 검증: node-check ×4 · verify 18/1/0 · smoke 30/0/0 · **student.js 무변경**(학교 lane 보존).
캐시버스터 현재값: `gamedata 20260713` · `student 20260713e`(학교) · `admin 20260713h` · `kiosk 20260713c`.

## ⏸️ 일부러 남겨둔 것 (교사가 버튼 한 번만 누르면 됨)
1. **[📦 이전]** — 옛 백업 7개(06-29~07-21)를 새 위치로 **복사**. 복사만 하고 옛 것은 보존.
2. **[🧹 정리]** — 중복 `quests` 269KB 삭제.
3. 위 둘이 끝나고 확인되면, 옛 `classRPG_v3/backups` 노드 삭제 → **전송량 6.4MB → 약 1.3MB(80% 감소)**.

> 사용자 부재 중 운영 데이터를 옮기거나 지우면 실패해도 아무도 못 잡으므로, 도구만 만들고 실행은 남겨둠.

## 판단해서 **하지 않은** 것
- **C2 보상 상수 불일치**(직접입력 daily 80/50 vs 템플릿·자동 35/25) — 수치가 바뀌는 **밸런스 건**이라 제외(사용자 방침).
- **C15/Q7 주 기준 통일**(일요일 vs 월요일) — 어느 쪽을 표준으로 할지가 **제품 결정**이고, 한국(KST) 기기에선 현재 무해.
- **C1 삭제 퀘스트 집계 왜곡** — 실데이터에 고아 로그 0건이라 현재 발생하지 않음.

## 🏫 학교 lane (student.js — 맥북은 계속 안 건드림)
- **R2** 금요일 보스 무제한(골드+EXP). `bossActive` 기본 false라 현재 미발동 → 켤 계획이면 먼저 수정
- **R8** 승인 보상 도착 알림·레벨업 연출 부재(전투 EXP 0 설계라 레벨업의 유일한 경로인데 연출이 안 뜸)
- **R9/B15** 미승인 대기가 '🎁 받기 가능'(승인 색)으로 표시 — `selfApplied`가 쓰는 곳 0인데 읽기만 함
- **B11** 자정 롤오버로 같은 게시글 중복 완료
- **C8** 열어둔 퀘스트 모달이 실시간 갱신 안 됨

## 다음 후보 (맥북 lane)
- B13 반려 기록·알림(학생 표시는 student.js 필요 → 학교와 조율)
- C4 커스텀 템플릿 능력치 저장(B1으로 일부 완화됨)
- **큰 것**: 일일 리셋을 "학생 로그인 부수효과"에서 명시적 트리거로 분리 → Q1·A5·C5·C16·B11 동시 해결. 고위험·승인 필요

---

## ⚠️ 미해결 — 학습게임 단원 구성이 실제 교과서와 다를 수 있음 (2026-07-30 사용자 지적)

PR #136으로 `curriculum.js`(4학년 1학기 3과목 15단원·86문항)를 넣었으나,
**사용자가 "영어·사회 단원이 그게 아니었던 것 같다"고 지적.** 확인 없이 진행한 것이 맞다.

| 과목 | 상태 | 사유 |
|---|---|---|
| 영어 | ❌ **거의 확실히 불일치** | 초등 영어는 **검정교과서** — 출판사마다 단원이 완전히 다름. 넣어둔 6단원은 실제 교과서 목차가 아니라 **작성자가 임의로 묶은 주제**다 |
| 사회 | ❌ **불일치 가능성 큼** | **2022 개정 교육과정이 2024년부터 3~4학년 적용**. 넣어둔 3단원은 2015 개정 기준일 가능성 |
| 수학 | 🟡 미확인 | 단원 구성이 비교적 안정적이나 2022 개정 기준인지 확인 안 함 |

**다음 세션에서 먼저 할 일**
1. 실제 기준 확보 — 학교 교육과정(연간계획) 문서 / 교과서 목차 / 지도서 중 하나.
   특히 **영어는 출판사명**이 필요(YBM·대교·천재 등에 따라 단원이 다름).
2. `curriculum.js`의 `units` 교체 + `BASE_PROBLEMS`의 `unitId` 동반 수정(둘이 짝이라 같이 바꿔야 함).
3. 문항 내용 자체(계산·정답)는 교육과정 무관하게 재사용 가능 — **단원 매핑만 다시 하면 됨**.

**참고**: 코드 구조는 단원 교체를 전제로 설계돼 있어(`customUnitNames` 오버라이드, unitId 기반 조회)
데이터만 갈아끼우면 되고 로직 수정은 불필요하다. 학생 풀이 화면(2단계)은 **단원 확정 후** 진행 권장.
