# 2026-07-03 작업 로그

## 2026-07-03 (기기: 학교 Windows) — PR #99 검토·머지 (씨앗/작물 에셋, PR-3)

- 한 일:
  - **PR #99 검토** (Codex 작업, `feat/asset-pr3-seeds-crops`):
    - 이미지 20장(씨앗 10 + 작물 10) 파일명 = 씨앗 id·crop 키와 1:1 정확 일치 확인, 전부 34KB 이하, 투명 배경·아동 친화 스타일 육안 확인.
    - `iconImg` 확장(4·5번째 인자 `fileId`/`fallbackIcon`) — 기존 몬스터 호출과 하위호환 유지 확인.
      crop은 GAME_DATA에 컬렉션이 없어 `fileId`로 우회, 폴백은 `cropIcon` 사용 — 올바른 설계.
    - 교체 5지점(상점 일반/돌연변이 씨앗 카드, 인벤토리, 미니 농장, 농장 모달) 스펙 준수,
      canvas `_drawYardFarm`·toast·confirm은 이모지 유지 확인 (asset_spec 금지사항 준수).
    - `sd`/`s` undefined 가드: iconImg 호출이 전부 `ready`(=sd 존재) 조건 안 → 기존 동작과 동일, 회귀 없음.
  - **발견·수정 1건**: 브랜치 분기점이 `b899f78`(#95 직후)라 Codex의 캐시버스터 갱신(c→d)이
    현재 main(이미 `20260702d`, #96에서 갱신)과 같은 값 → 병합 시 무효화됨(신 JS + 구 버스터).
    → PR 브랜치에 `student.js?v=20260703` 갱신 커밋 추가. (Codex 보고의 smoke PASS 27도 구기준 스크립트 탓 — 구조 문제 아님)
- 검증 (main과의 병합 시뮬레이션 트리에서 실행):
  - `node --check student.js` OK / verify-safety **PASS 18 · REVIEW 1 · FAIL 0** /
    smoke-test **PASS 30 · REVIEW 0 · FAIL 0** (현행 기준선 충족). Firebase write 없음.
- 결과: PR #99 squash 머지(사용자 사전 승인: "문제 없으면 머지"), 운영 반영 GET 확인.
- 다음 할 일:
  - **PR-4 (장비 에셋 80장)**: 조건부 승인 — 상점·인벤토리 카드만 적용, `buildCharSVG`·캐릭터 착용 렌더 절대 금지.
    지시문에 명시할 것: ① `assets/equipment/` 파일명 = 장비 id(e_*) 그대로, ② iconImg 기본 경로(장비는 GAME_DATA.equipment에 있어 fileId 불필요),
    ③ 판매 confirm·toast 텍스트는 이모지 유지, ④ 최신 main에서 분기 + `?v=` 갱신, ⑤ 기준선 smoke PASS 30.
- 주의/미해결:
  - 농장 모달의 씨앗 선택 칩은 이모지 유지 중(스펙 범위 밖) — 후속 미세 정리 후보.
  - 농장 스타듀 재구성 설계 초안 작성됨(바탕화면 클로드코드\농장_재구성, 코덱스 검토 전) — 로드맵 0단계가 본 PR.
