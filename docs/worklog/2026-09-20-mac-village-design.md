# 2026-09-20 (기기: 맥북) — 마을 디자인 담당

> 주말 병렬 체제. 이 세션은 처음엔 ④구역(밸런스 결정표·상용화 격차, 읽기 전용)이었고 → 선생님 확인("ㅇㅇ") 뒤 **마을 디자인 담당**으로 바뀌었다.
> 기준 = `origin/feat/village-45`(59차), PR base 도 거기. **`?sid=guest` 로만** 열었다 — 운영 DB 요청 0(네트워크 기록 확인), student/admin 화면 안 열음, Firebase 접근 0, 머지 0.

## 한 일
| PR | 무엇 | 상태 |
|---|---|---|
| #422 | (역할 바뀌기 전) 선생님 결정표 + 상용화 격차 문서 — base=main, **[초안·보류]** | 초안 |
| #433 | 칸을 덮는 것은 칸 끝까지 — 꽃밭·울타리·돌담·잔디·자갈·덤불 (DESIGN-TILE-1) | 보스 머지 |
| #435 | 나무 크기 비례 — `GLB_SCALE` (DESIGN-SCALE-1) | 보스 머지 |
| #437 | 주민 옷 색 밝은 네 색 (DESIGN-FOLK-1) | 열림 |
| 이 PR | 생김새 점검·빠진 조각 제안·집 모습 설계안 [`village-design-20260920.md`](../village-design-20260920.md) + 이 기록 | 열림 |

## 고친 기존 줄 지도 (월요일 학교 판과 3-way 병합용)
줄 번호는 **59차(`4fbd3a9`) 기준**. 전부 `village/index.html`.

| PR | 59차 줄 | 어디 | 바뀐 것 | 새 블록인가 |
|---|---|---|---|---|
| #433 | 317 | `K.lawn` 첫 box | `CS*.94` → `CS` | 기존 줄 |
| #433 | 337 | `K.gravel` 첫 box | `CS*.94` → `CS` | 기존 줄 |
| #433 | 343~351 | `K.flower` parts 9줄 | 통째 교체(잎 깔개 + 꽃머리 13) + 주석 3줄. **머리줄(nm·grp·need·w·h·g·cap) 그대로** | 기존 줄 |
| #433 | 355~357 | `K.bush` 공 3개 | 크기·자리 + 주석 1줄 | 기존 줄 |
| #433 | 439~443 | `K.fence` | 기둥 3→4, 가로대 `6.6` → `CS*2` + 주석 1줄 | 기존 줄 |
| #433 | 445~446 | `K.wall` | 몸통 `6.6`·갓돌 `6.8` → `CS*2` | 기존 줄 |
| #435 | 1266 앞 | `const GLB_PARTS` 위 | `GLB_SCALE` 주석 3줄 + 표 1줄 | **새 블록** |
| #435 | 1269 | `glbParts()` return | `s:[1,1,1]` → `s:[k,k,k]` + `const k` 한 줄 | 기존 줄 1 |
| #437 | 2047 | `const TOPC` | 색 4개 + 주석 2줄 | 기존 줄 1 |

찾는 법: 세 PR 모두 주석에 `[DESIGN-TILE-1]` · `[DESIGN-SCALE-1]` · `[DESIGN-FOLK-1]` 표식이 있다 — `grep -n "DESIGN-" village/index.html`.
규칙 필드(nm·grp·need·w·h·cap)·저장 형식 v2·palette·sync.js 는 **한 줄도 안 건드렸다.** 줄 끝 CRLF 유지(PR 마다 diff 가 몇 덩이로만 나오는 것 확인). 편집은 파이썬으로 바이트를 읽어 `\r\n` 으로만 붙였다(편집기로 열면 LF 가 섞일 수 있다).

## 새 종류 id — **이번엔 0개.** 아래는 제안서의 안(만들기 전에 여기 먼저 적는다)
`gate_f` `gate_w`(문) · `fence_c` `wall_c`(모퉁이 — 자동 이음 결정 뒤) · `flower_p` `flower_y` `flower_w`(꽃밭 색 변형) · `tree_top` `tree_box` `tree_pop`(나무 실루엣) · `pave_s` `pave_b`(광장 바닥 1×1) · `curb`(화단 턱).
프로그램 담당이 같은 이름을 쓰려면 여기 먼저 적을 것.

## 모양 규칙 (자세히는 제안 문서 ①)
- **칸을 덮는 것**: 바닥판 100% · 높이 ≤ 0.4 · 무늬 간격이 칸 경계를 넘어도 같게.
- **칸을 잇는 것**: 긴 축은 칸 끝까지.
- **서 있는 물건**: 칸의 40~70% · 바닥판 없음.
- **무게는 삼각형 수**(청크 합치기 — 부위·재질 수는 draw call 과 무관). box 12 · cone 12 · cyl 24 · sph 60△.

## 잰 값 (맥북 · 1366×610 · `#full` = 집 30채·물건 57,733개 · `__bench(200)` ×3 = renderer.info)
| | draw call | 삼각형 | 일한 평균 | 콘솔 오류 |
|---|---|---|---|---|
| 59차 그대로 | 237~239 | 324,722~327,652 | 1.1ms | 0 |
| #433 뒤 | 234~252 | 303,578~322,762 | 0.7ms | 0 |
| #435 뒤 | 235~255 | 325,234~347,404 | 1.1ms | 0 |

`__bench` 는 카메라를 돌리며 재서 화면에 걸리는 청크 수만큼 흔들린다 — **물건당 삼각형**이 믿을 값이다: 꽃밭 348→168 · 울타리 60→72 · 나머지 그대로. 맥북 ms 는 참고일 뿐, **크롬북 실측은 아직 아무도 안 했다.**
주의: `__benchLoop` 의 `그리기`·`삼각` 은 화면 밖 버퍼라 0~1 로 나온다(쓸 수 없는 값). `__bench` 나 `__state().drawCalls` 를 쓸 것.

## 점검할 때 쓴 방법 (다음 사람용)
- 서버: `python3 -m http.server 8797` (저장소 루트) → `village/index.html?sid=guest`. 장면 훅: `#demo`(예시 마을) · `#full`(가득) · `#adv`(고급 12종) — 셋 다 저장 안 함.
- **해시만 바꾸면 다시 안 읽는다** — 쿼리에 아무 값(`&s=1`)을 같이 바꿔야 새로 열린다.
- 원본 해상도 캡처는 Playwright(앱 안 브라우저 창 캡처는 800px 로 줄어 세부가 안 보인다).
- 시험 배치: v1 저장 형식 `{v:1,w:256,h:256,items:[{id,x,y,rot}],houses:{칸번호:[사람,층,-1]}}` 을 **로컬 시험 브라우저의** `localStorage['rpg.village.guest']` 에 넣고 다시 열면 된다(v1→v2 로 바꿔 읽는다).
- 훅: `__view(배율)` · `__pan(dx,dy)` · `__setTime(ms)`(44000 ≈ 낮 12시) · `__setSpeed(0)` · `__bench(n)` · `__gfx()`.
- 집 모습 3단계 모형은 `buildChunk` 에 임시로 색 바꾸기를 끼워 찍고 **되돌렸다**(커밋 없음).

## 월요일 부탁
1. 학교 최신판과 합칠 때 위 줄 지도 9곳만 보면 된다. 학교 판이 같은 물건의 parts 를 고쳤으면 **학교 판 우선**, 모양 규칙 표만 맞춘다.
2. **크롬북 실측**: 기본 품질(그림자 없음)에서 나무·사람이 떠 보이는지(발견 5), `#full` 프레임.
3. 집 glb 원본(블렌더)이 학교 PC 에 있는지 — 2·3층 실루엣(설계안 ③)은 모델을 다시 뽑아야 한다.
4. 제안서 ②의 추천 순서(C 꽃밭 색 변형 → A 문 → E·F → D → G) 중 고를 것.
5. 로컬에만 있는 것(커밋 안 함): `.claude/launch.json`(8797 정적 서버) · `.playwright-mcp/`(캡처 원본).

---

## 오후 추가 (보스 전달: 선생님 "꽃밭이 저게 뭐야 — 롤러코스터 타이쿤처럼 풍성하게" · 3D 구조 그대로 · 시뮬레이션 세상)

| PR | 무엇 | 상태(기록 시점) |
|---|---|---|
| #437 | 주민 옷 색 (DESIGN-FOLK-1) | 열림 |
| #440 | 생김새 점검·빠진 조각·집 모습 설계안 + 이 worklog | 보스 머지 |
| #445 | **풍성한 꽃밭** — 한 칸 꽃 28송이·높낮이 3단·8벌을 칸 좌표로, 한 칸 전체가 기하 하나(306△) (DESIGN-FLOWER-2) | 열림 — **선생님 방향 확인용** |
| #448 | 놓는 방식 전수표 47종 [`village_place_table.md`](../village_place_table.md) + 모양 규칙 한 장 [`village_shape_rules.md`](../village_shape_rules.md) | 열림 |

### #445 줄 지도 — **기존 줄 수정 0**
| 어디(현재 base) | 무엇 |
|---|---|
| `for (const g in GEO) { … }` 닫는 줄 아래 | **새 블록 55줄** `[DESIGN-FLOWER-2]` — `buildFlowerBeds()` · `FLOWER_PARTS` · `__flowerBeds()` |
| `function partsOf(rec) {` 바로 아래 | **한 줄 추가** — 꽃밭이면 `FLOWER_PARTS[(x·7 + y·13) % 8]` |

`K.flower`(#433 의 parts)는 그대로 예비. 주소 끝 `#fb=0` 이면 옛 모양.

### 잰 값 — 집 50채 + 꽃밭 100칸 + 주민 100명 · CPU 4배 감속(CDP) · 1366×610
| | draw call | 삼각형 | 일한 평균 |
|---|---|---|---|
| 전(`#fb=0`) | 76 | 99,520 | 1.7ms |
| 후 | 73 | 113,314 | 1.7ms |

### 바로잡음
- 앞 기록과 제안 문서의 "cone 8△" 는 **12△** 가 맞다(`ConeGeometry(1,1,4)` = 옆 8 + 밑 4). 이 PR 에서 고침.
- 측정에 임시 파일을 `/tmp` 에 한 번 만들었다가 바로 지웠다(저장소 밖, 남은 것 없음).

### 다음 (방향 확인 뒤)
1. 꽃밭 색 변형 4~6종 — 새 id 안: `flower_p`(분홍) · `flower_y`(노랑) · `flower_w`(흰) · `flower_v`(보라). 같은 함수에 색 표만.
2. 꽃밭 위 나무용 '가운데 비운 벌' · '깔개만 있는 벌'(전수표 ③-1).
3. 같은 잣대의 '빈약함 목록': 덤불(공 3개 → 잎 덩어리 기하) · 잔디밭(풀포기 6개 → 칸마다 다른 풀 기하) · 연못(물풀·연잎 없음) · 광장(무늬 없음) · 나무(실루엣 3종뿐).
4. 월요일: 가을·겨울 색을 실제로 흘려 보며 꽃밭 잎 색 확인(시험 훅은 멈춘 상태에서 계절색을 다시 안 칠한다).

---

## 저녁 추가 — 꽃밭 가족 (선생님: "꽃은 한 종류가 아니야 · 종류도 많았으면 · 목표에 따라 해금되는 꽃")

| PR | 무엇 | 상태(기록 시점) |
|---|---|---|
| #456 | 꽃밭 방향 다시(점 → 덩어리·색 무리) 시안 A·B·C | 머지 |
| #466 | 꽃밭 여섯 종 + 🌷 꽃 탭 (DESIGN-FLOWER-4) | 머지 |
| #468 | 정원 가족 시안·목록 [`village_garden_family.md`](../village_garden_family.md) | 머지 |
| 이 PR | **꽃밭 14종 + 인구 문턱 해금** (DESIGN-FLOWER-5) | 열림 |

### 새 종류 id (만들기 전에 여기 먼저 — palette 는 뒤에 붙는다)
FLOWER-4: `flower_hb` `flower_tr` `flower_ty` `flower_wd` `flower_wv`
FLOWER-5: `flower_hy` `flower_to` `flower_hw` `flower_sf`(해바라기) `flower_lv`(라벤더) `flower_t2`(알록달록 튤립) `flower_rb`(무지개 들꽃) `flower_nt`(달빛 — 밤에 빛남)
예정(정원 가족): `flower_hr` `flower_hv` · 테두리 화단 `flower_bk` `flower_st` `flower_tb` · `topi_cone` `topi_ball` `topi_two` `topi_cyl` `topi_box` · `hedge`

### 꽃 ↔ 열리는 때 (지금은 인구 문턱 = 목표 '인구 N명'. 프로그램 담당의 '목표 → 열리는 것' 표가 생기면 `FLOWER_KINDS` 의 `pop` 을 그리로 옮긴다)
| 열리는 때 | 꽃밭 |
|---|---|
| 처음부터 | 꽃밭(분홍 둔덕) · 튤립밭 · 들꽃밭 |
| 인구 10 | 노란 꽃밭 · 노란 튤립밭 |
| 인구 30 | 파란 꽃밭 · 보라 들꽃밭 · 주황 튤립밭 |
| 인구 50 | 흰 꽃밭 · 해바라기밭 |
| 인구 80 | 라벤더밭 · 알록달록 튤립밭 |
| 인구 120 | 무지개 들꽃밭 |
| 인구 160 | 달빛 꽃밭(밤에 은은히 빛난다) |

### 배운 것
- **머지된 PR 의 브랜치에 이어 쓰지 않는다.** #456 에 뒤늦게 얹은 커밋이 squash 에서 빠져 #466 으로 다시 올렸다. 추가 작업은 늘 최신 base 에서 새 브랜치.
- **점을 늘리면 다시 점박이가 된다.** 라벤더를 가는 꽃대 15개로 뽑았다가 굵은 12개로 고쳤다. 풍성함 = 덩어리·겹침·질서.
- 꽃 14장 + 도구 4장은 1366 한 줄에 안 들어간다(두 줄). CSS 로 잠긴 카드를 좁혀 봤지만 효과가 없어 되돌렸다 → '카드 = 모양, 띠 = 색' 또는 '다음 단계만 보이기'가 답(프로그램 담당).
