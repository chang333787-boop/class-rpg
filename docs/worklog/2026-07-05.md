# 2026-07-05 작업 로그

## 2026-07-05 (기기: 맥북, Claude Code) — 코드 심층 분석 + XSS 보안 수정(S-3)

### 한 일
- **전체 코드 심층 분석(read-only)**: student.js/admin.js/kiosk.js/gamedata.js 3개 병렬 분석 + 아키텍처·상용 SaaS 리뷰.
  보고서는 맥북 Desktop `RPG_코드분석_버그오류_개선방향_20260705.md`. 핵심 발견(심각도순):
  - **S-1 (Critical)** 인증/rules 부재 + 평문 비밀번호. Firebase Auth 호출 0건, 레포에 rules 파일 0건. read-only GET으로 `classRPG_adminPw` 평문 공개 읽힘 확인. **상용화 법적 게이트.**
  - **S-2 (Critical)** 통짜 `saveStudent`(전체 노드 set) + `_saving` 500ms 리스너 억제 → 교사 승인과 학생 저장 경합 시 승인 유실·pending 부활(이중지급).
  - **S-3 (Critical)** 저장형 XSS — 학생 작품 제목/설명·독서 감상이 미이스케이프로 innerHTML/onclick 삽입. admin 작품 승인 onclick은 교사 admin 컨텍스트 실행 가능.
  - High: S-4 kiosk pendingRewards 배열 통짜 set 경합 / S-5 기기 시계 조작 → 무한 골드 / S-6 import·rollback이 백업 파괴 / S-7 저장 실패 무피드백 / S-8 독서 이중 승인 경로 / S-9 접속만료×전투 크래시 / S-11 무한배틀 새로고침 골드 증발.
  - Medium/Low: S-12 kiosk 이름 미이스케이프(버튼깨짐)·주 시작 기준 불일치·뱃지 카운트·링크 href 스킴(S-20)·deleteBook 중복정의 등.

### 반영 — S-3 XSS 이스케이프 (PR #102, `d31ff71`)
- **admin.js**: 작품 제목/설명(작품 승인), 이미지 src + onclick 인자(escJsAttr), 독서 감상(승인목록 1016·대시보드 277), 주간 자기점검(느낀점/잘된점/어려운부분).
- **student.js**: 작품 탭 제목/설명/과목(대기·전시), 독서 상세(인물/줄거리/느낀점).
- 표시 경로만 escHtml/escJsAttr. **저장값/DB/보상 로직 0 변경**(DB 저장 문자열엔 미적용 — 오염 방지).
- **캐시버스터 갱신**: `admin.js?v=20260705`, `student.js?v=20260705`. **kiosk는 미변경(20260702 유지).**
- smoke-test.mjs 버전 스냅샷 맵 동기화(student/admin → 20260705, kiosk 20260702).
- 검증: node --check ×4 OK / verify-safety PASS 18·REVIEW 1·FAIL 0 / smoke-test PASS 30·REVIEW 0·FAIL 0. Firebase write 없음.

### ⚠️ 학교 세션 주의 — PR #101(장비 에셋 80장) rebase 필요
- **#101이 #102와 같은 3파일(student.js·student.html·scripts/smoke-test.mjs)을 건드림.** #102가 먼저 main에 들어갔으니 #101은 rebase 시 충돌 가능:
  - `student.html`: `student.js?v=` 라인 — **20260705로 유지**(#102 값). #101이 추가 갱신하면 더 높은 날짜로.
  - `scripts/smoke-test.mjs`: 버전 스냅샷 맵 2곳(82-83행 fetch URL, 138행 jsVer) — student/admin=20260705 유지, kiosk=20260702.
  - `student.js`: #102는 **작품/독서 렌더** 이스케이프, #101은 **상점/인벤 장비** 렌더 → 보통 다른 구역이라 자동 병합. 충돌 시 양쪽 다 살릴 것(이스케이프 + iconImg).

### 다음 할 일 (안전한 순서 — #101 머지 후 재개)
- **A트랙(안전·표시 경로)**: S-12 kiosk 이름 이스케이프(escJsAttr 이식) / S-20 링크 href 스킴 화이트리스트 / S-7 저장 실패 `.catch`+안내.
  → 이들도 캐시버스터+smoke-test.mjs를 건드리므로 **#101이 main에 들어간 뒤** 진행(충돌 누적 방지).
- **B트랙(고위험·승인 필수)**: S-1 Auth+rules / S-2 저장 원자성.
  - ⚠️ **Firebase MCP가 현재 RPG(`class-rpg-6f409`)가 아니라 가지(`picturebook-8731f`)를 가리킴.** RPG 레포엔 firebase.json/rules 파일도 없음.
  - **rules를 auth 도입 전에 배포하면 학생 전원 즉시 로그인 불가.** 안전 순서: ①클라 Auth(익명) → ②rules 스테이징 validate → ③앱 통과 확인 → ④배포. 반드시 승인 후.

### 미해결/요검증
- S-9 접속만료×전투 크래시, S-10 선공 즉사 전투 잠금, S-11 무한배틀 새로고침 골드 소실 — 실환경/하니스 재현 필요.
- emotionLogs/questLogs 무제한 누적 정리 정책 부재.
