# 2026-09-15 (기기: 학교 Windows) — 작품 올리기에 학습지 스캔 연결 (SCAN-LINK-1)

> 담당: 학습 조수(`st`). 사용자 지시 "애들 학습지 올릴 때 스캔되는 기능" ③. 보스 판정: 붙는 곳 = 작품 올리기 흐름 재사용 · 순수 JS · **기본 컬러**(색연필·형광펜), 흑백은 선택. #362(시험 페이지) 위.

- **변경**: `student.html` 작품 올리기 폼에 "📄 학습지 스캔하기" 단추 · `student.js` 스캔 iframe 열기/닫기·결과 받기·제출 분기 · `admin.js` 작품 필터 칩 "📄 학습지" + 대기 카드 표시 · `scan/index.html`·`scan.js` 기본 모드 컬러 / 버스터 `student.js`·`admin.js` `20260915str`, `scan.js`·iframe 주소 `20260915stq`.
- **운영 쓰기 0**(시험은 가짜 Storage·DB), Storage 규칙 변경 0(같은 `artworks/` 경로), 보상·좋아요·승인은 작품과 같다(`kind:'worksheet'`만 다름 — 교사 승인 시 `saveArtwork`가 kind를 그대로 옮긴다).

## 재현 → 수정 → 재현 안 됨
- **재현**: 작품 올리기에는 사진 고르기만 있어서 종이 학습지가 비스듬·그림자 진 채로 올라갔고, 교사 화면에서 작품과 학습지를 가를 수 없었다.
- **수정**:
  1. 단추 → 전체화면 iframe `scan/index.html` → 아이가 찍기·테두리 확인·올리기
  2. scan이 `postMessage({type:'scan-result', blob})` → 부모가 **같은 출처·`image/jpeg`·600KB 이하**만 받음 → 미리보기 표시·오버레이 닫기·제목 칸으로
  3. 제출: 스캔본이 있으면 `resizeImage` 대신 그 Blob을 **같은 Storage 경로**로, `kind:'worksheet'`, 라벨 "📄 … 학습지 제출", 완료 알림도 학습지 문구
  4. 사진을 새로 고르면 스캔본은 풀림(사진이 이김), 제출 뒤 초기화
  5. 학생 갤러리 태그 "학습지", 교사 작품 관리 필터 칩 "📄 학습지"·대기 카드 표시
- **재현 안 됨** (하네스: 실제 student.html 폼 + student.js 해당 함수, 가짜 Storage·DB, 375px):
  - 단추 표시 → iframe 열림(기본 모드 컬러) → 합성 그림자 사진 넣기 → 테두리 확인 → 스캔 → 올리기 → **부모가 219KB JPEG 받음·오버레이 닫힘·미리보기·안내**
  - 제목 넣고 제출 → Storage `artworks/harness_….jpg` 223KB `image/jpeg` · pendingReward `kind:'worksheet'`·30EXP·20G · 스캔본 초기화
  - 사진 고르면 스캔본 풀림 → 같은 제출이 `kind:'lesson'`·"🎨 작품 제출"
  - 사진·스캔 둘 다 없음 → "사진을 선택하거나 학습지를 스캔해 주세요!"
  - 이상한 메시지(text/plain Blob) → 거부
  - 제목 `<b>…</b>`: 라벨은 `toast`·`notify`가 `textContent`, 갤러리·교사 카드는 `escHtml` → 태그로 해석되지 않음
- 캡처(375): 수정 전 폼(사진 선택만) / 수정 후 폼(학습지 스캔하기 단추 추가)
- `verify-safety` PASS 18 · REVIEW 1 · FAIL 0 / `smoke-test` PASS 29 · REVIEW 0 · FAIL 0 / `unit` 105/0

## 남은 것
- 실제 종이 사진 5장으로 재시험(사용자가 `파일함\`에 주면).
- 크롬북 실측(G3 때).
