# 2026-09-03 (기기: 학교 Windows) — 영어 복습앱 연동 (ENGLISH-LINK-1)

> 담당: 영어앱 Claude 세션. 같은 날 수채화 세션이 `feat/watercolor-page`(worktree `class-rpg-wt-watercolor`)에서 `watercolor/`만 작업 중 — 파일 겹침 없음.

- **브랜치**: `feat/english-link` (worktree `C:\Users\USER\projects\class-rpg-wt-english`, base `origin/main` 71c114f)
- **대상 파일**: `student.js`, `student.html`(firestore-compat 스크립트 1줄 + `?v=` 캐시버스터), `scripts/smoke-test.mjs`(student.js 버전 맵만 동기화)
- **안 건드림**: gamedata.js · admin.js · curriculum.js · kiosk.* · Firebase 루트 구조 · DB 규칙
- **새 Firebase 노드**: 없음. 학생 레코드에 필드 `englishRewards`(객체: 이미 준 보상 키) 추가만 — `students/<id>` 통째 set 하는 기존 `DB.saveStudent` 경로 그대로.
- **외부 읽기**: 영어앱 Firestore(프로젝트 `jeongrim-equip`, 경로 `english/1234/students/jeongrim_<학생이름>`)를 **읽기만**. compat SDK로 두 번째 앱(`firebase.initializeApp(cfg,'english')`) 초기화 — 기본 앱/RTDB와 분리.

## 한 일
1. `syncEnglishRewards()` — `enterGame()` 직후 1회 + 이후 5분 간격. 영어앱 기록을 읽어 아래 보상을 `CUR.pendingRewards`에 push(emotion 보상과 같은 객체 형식, `approved:true` = 즉시 수령 가능). 중복은 `CUR.englishRewards[key]`로 차단.
   - 오늘 영어앱 학습함(`lastDay === 오늘`): 20 EXP / 15 G — 하루 1회
   - 단어·표현 테스트 90% 이상: 30 EXP / 20 G — 테스트 회차마다 1회
   - 단원 숙달 100%(`lessons[L1..L7]===100`): 80 EXP / 50 G — 단원당 1회
2. 오늘의 학습 과목 선택에서 **영어 카드 → 외부 학습 앱 카드(EXTERNAL_STUDY 배열: 🔤 영어 복습앱 · 🎨 수채화 기초 `watercolor/index.html?sid=<id>`)**로 교체(수채화 세션과 합의, 2026-09-03 2차 커밋)(새 탭, `?name=<학생이름>&k=1234` 자동 입장). RPG 내부 영어 문항(`curriculum.js` en4-1-*)은 삭제하지 않고 진입점만 막음(교사 학습범위 화면에는 여전히 보임 — 후속 결정).
3. 학생 이름이 영어앱과 다르면 조용히 건너뜀(오류 토스트 없음). 인터넷/SDK 실패도 게임 진입을 막지 않음(try/catch).

## 검증
- `node --check student.js` / `node scripts/verify-safety.mjs`(기대 PASS 18 · REVIEW 1 · FAIL 0) / `node scripts/smoke-test.mjs` — 결과는 아래 "검증 결과"에.
- Firebase write 없이 정적 검증만. 운영 데이터 미접촉.

## 검증 결과 (2026-09-03)
- `node --check student.js` → OK
- `node scripts/verify-safety.mjs` → **PASS 18 · REVIEW 1 · FAIL 0** (기대값과 동일)
- `node scripts/smoke-test.mjs` → **PASS 33 · REVIEW 0 · FAIL 0**
- 변경 파일: student.js(+연동 블록·enterGame 훅·과목 선택 카드), student.html(firestore-compat 1줄·`?v=20260903a`), scripts/smoke-test.mjs(버전 맵), 본 worklog
- 브라우저 실로그인 검증은 안 함(운영 학생 레코드에 write가 발생하므로 규칙상 금지). 머지 전 교사가 테스트 학생으로 1회 확인 권장.

## 다음 할 일
- 사용자 승인 후 main 머지 → GitHub Pages 반영(자동 머지 금지 규칙).
- 외부 학습 입구를 한 곳으로 모을 때 수채화 항목 추가: `watercolor/index.html?sid=<studentId>` (수채화 세션 요청).
- admin 학습범위 화면의 영어 단원 노출 여부, 보상 단가 조정은 교사 결정.

## 주의/미해결
- 영어앱과 RPG의 학생 이름이 정확히 같아야 매칭됨(공백·번호 표기 주의).
- `approved:true` 자동 지급은 emotion 보상 선례를 따름. 교사 승인형으로 바꾸려면 `approved` 제거만 하면 됨.
